# FOL Audit — 2026-07-21 (multi-agent scan)

8 parallel scanners (legacy, manager-hardening, docs-truth, compiler quality, tooling quality,
std surface, tests, build/release) -> 118 findings -> 3 adversarial verifiers.
**75 CONFIRMED, 3 rejected, 39 low severity unverified.** Several highs were reproduced live by the verifiers.

Ground rule applied throughout: greenfield, zero backward compatibility — migration shims and
`use X instead` hints count as defects.


## CONFIRMED — HIGH (15)

### 1. fol_model 'std'/'mem' migration shims with 'no longer accepts... use X instead' hints
- **file**: `lang/execution/fol-build/src/executor/graph_methods.rs`
- **category**: legacy-leftover
- **evidence**: graph_methods.rs:869-874 returns "artifact fol_model no longer accepts 'std'; use 'memo' and declare bundled std through build.add_dep(...)" and :875-880 returns "artifact fol_model no longer accepts 'mem'; use 'memo'". The generic arm at :881-889 already rejects unknown values with "artifact fol_model must be one of: core, memo". The 'std' hint text is asserted verbatim by a test in lang/execution/fol-build/src/eval/tests/source_configs.rs (assert_eq at ~line 199-202). Textbook migration-hint shims under the greenfield no-legacy hard rule.
- **detail**: graph_methods.rs:869-880 special-cases the removed fol_model values: 'std' returns "artifact fol_model no longer accepts 'std'; use 'memo' and declare bundled std through build.add_dep(...)" (line 872) and 'mem' returns "artifact fol_model no longer accepts 'mem'; use 'memo'" (line 878). These are exactly the migration hints the greenfield rule forbids; the catch-all arm right below (line ~884) already rejects unknown values with "artifact fol_model must be one of: core, memo". The shim behavior is additionally locked by a test at lang/execution/fol-build/src/eval/tests/source_configs.rs:201. Delete both special arms and the locking assertion; let 'std'/'mem' fall into the generic unknown-value error.

### 2. fol-self toolchain manager has zero behavioral/integration coverage
- **file**: `lang/tooling/fol-self/src/main.rs`
- **category**: test-coverage
- **evidence**: The only tests are 7 string-helper unit tests at main.rs:721-775 (parse_toolchain_pin x4, normalize_version_dir, looks_like_version, parse_link_manifest). Nothing exercises resolve_toolchain (:309), the dispatch/exec path (:373-427), self_install (:461), install_from_source (:487), install_from_network (:531) or fetch (:572). lang/tooling/fol-self has no tests/ dir; workspace-wide grep for CARGO_BIN_EXE finds only CARGO_BIN_EXE_folc — no test spawns the `fol` manager binary. tests.yml runs `make verify` -> `cargo test --workspace`, reaching only those unit tests; release.yml builds fol-self (:58) but never install-smoke-tests the staged tarball. The {folc, std/, runtime/} archive contract between release.yml and install_from_network is entirely unexecuted. Three proven-by-probe bugs in this same file (link name, config parser, +spec self) illustrate the cost.
- **detail**: The only tests are 7 pure string-helper unit tests in main.rs:722-775 (parse_toolchain_pin, normalize_version_dir, looks_like_version, parse_link_manifest). Nothing exercises resolve_toolchain (main.rs:309), run_toolchain exec dispatch (main.rs:410), self_install (main.rs:461), install_from_source (main.rs:487), install_from_network curl/wget fallback (main.rs:531/573/582), or the FOL_HOME/toolchains layout. No test anywhere spawns the manager binary — grep for CARGO_BIN_EXE_fol (non-folc) finds nothing. tests.yml only reaches fol-self via 'cargo test --workspace' unit tests + clippy; release.yml builds it (line 58) but never smoke-tests that the staged tarball actually installs via 'fol self install'. The critical folc/std/runtime archive-layout contract between release.yml and fol-self is completely unexecuted.

### 3. `fol self remove <spec>` deletes arbitrary directories via path traversal (proven)
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: self_remove (main.rs:659-678) takes args[0] raw, passes it through normalize_version_dir (220-223, which only prepends 'v'), joins onto toolchains_root and calls fs::remove_dir_all on any `is_dir()` result (670-673). No looks_like_version (225-228) call, no component validation. REPRODUCED in a scratchpad sandbox with target/release/fol: `FOL_HOME=$SB/home fol self remove '0.2.0/../../../victim/precious'` printed "removed toolchain 0.2.0/../../../victim/precious", exit 0, and `$SB/victim/precious` (containing file.txt) was recursively deleted — verified $SB/victim empty afterward. The .toml branch (663-668) has the identical hole for arbitrary *.toml file removal.

### 4. `install --from` destroys the existing toolchain before validating the runtime source; failure leaves a corrupt toolchain that reports healthy (proven)
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: install_from_source pre-checks only folc (491-496) and lang/library/std (497-500), then remove_dir_all's the destination (503-506), copies folc (509) and std (511), and only at 513-521 touches lang/execution/fol-runtime and errors out. REPRODUCED: healthy $FOL_HOME/toolchains/v0.2.0 containing {folc, MARKER, std} plus a --from tree with target/debug/folc and lang/library/std but no lang/execution/fol-runtime -> `fol self install 0.2.0 --from $FAKE` printed "error: cannot copy runtime Cargo.toml: No such file or directory (os error 2)", exit 1, and the destination afterward held {folc, runtime(empty), std} with MARKER gone. `fol self list` then still printed "v0.2.0" as an installed toolchain, because installed_toolchains (327-343) and resolve_installed (296-307) only test that `folc` is a file. Same window applies to any mid-copy failure.

### 5. Compiler panics on loop iteration over a first-class chn[rx,T] receiver value
- **file**: `lang/compiler/fol-lower/src/exprs/flow.rs`
- **category**: panic-reachable-from-user-input
- **evidence**: PANIC REPRODUCED. lower_channel_iteration (flow.rs:706-710) requires LoweredType::Channel and otherwise hits `unreachable!("channel_binding_local verifies the lowered type")` at :709, but channel_binding_local (fol-lower/src/exprs/expressions.rs:84-94) admits Channel | ChannelSender | ChannelReceiver, and typecheck allows receiver iteration: controlflow.rs:361 calls channel_receiver_element_type, which accepts CheckedType::ChannelReceiver (fol-typecheck/src/exprs/helpers.rs:717-718, with the comment "a first-class `chn[rx, T]` receiver value receives through the moved unique handle"). Repro run with target/release/folc on a package declaring build.add_dep({alias="std", source="internal", target="standard"}) + fol_model="memo", src/main.fol = `var channel: chn[int]; var receiver: chn[rx, int] = channel[rx]; for (value in receiver[rx]) { var x: int = value; };` -> `thread 'main' panicked at lang/compiler/fol-lower/src/exprs/flow.rs:709:9: internal error: entered unreachable code: channel_binding_local verifies the lowered type`. book/src/900_processor/200_corutines.md:92-93 explicitly promises "receiver iteration". Note flow.rs:895-903 has the same unguarded pattern in the select path.
- **detail**: PROBE-CONFIRMED. `lower_channel_iteration` at fol-lower/src/exprs/flow.rs:707-709 requires `LoweredType::Channel` and hits `unreachable!("channel_binding_local verifies the lowered type")` otherwise — but `channel_binding_local` (fol-lower/src/exprs/expressions.rs:84-95) deliberately admits Channel, ChannelSender AND ChannelReceiver, and typecheck explicitly ALLOWS iterating a first-class receiver: `channel_receiver_element_type` (fol-typecheck/src/exprs/helpers.rs:717-718) accepts `CheckedType::ChannelReceiver`. Repro (folc code check, no build needed): `var channel: chn[int]; var receiver: chn[rx, int] = channel[rx]; for (value in receiver[rx]) { ... }` -> `thread 'main' panicked at lang/compiler/fol-lower/src/exprs/flow.rs:709:9: internal error: entered unreachable code`. The book (book/src/900_processor/200_corutines.md:91-93) explicitly promises 'receiver iteration' works, so this is a spec-promised surface that panics instead of compiling. Fix: handle `LoweredType::ChannelReceiver` in lower_channel_iteration (runtime FolReceiver already exposes receive_optional per the comment at expressions.rs:175-177).

### 6. Direct compile route skips the package-store fallback chain (works in workspace mode, fails in direct mode)
- **file**: `lang/tooling/fol-frontend/src/direct.rs`
- **category**: inconsistency
- **evidence**: direct.rs:96-99 and 390-392 build ResolverConfig verbatim from `config.std_root.clone()` / `config.package_store_root.clone()` (populated straight from CLI roots in dispatch.rs:127-156) with no .fol/pkg or available_bundled_store_root fallback, unlike resolver_config in compile/mod.rs:1564-1588. REPRODUCED in one directory with a `use std: pkg = {"std"};` program: `folc code check` (workspace route) proceeds into std sources (fails later at std/fmt/root.fol on an unrelated missing internal dep), while `folc code check src/main.fol` (direct route) fails immediately with R1001 "resolver pkg import 'std' requires an explicit package store root" (raised at fol-resolver/src/imports.rs:149-157). Same program, contradictory results by path spelling.
- **detail**: direct.rs builds ResolverConfig straight from CLI overrides (lang/tooling/fol-frontend/src/direct.rs:96-99 and 390-392; populated in lang/tooling/fol-frontend/src/dispatch.rs:127-156) with no fallback to the workspace-local `.fol/pkg` store or fol_package::available_bundled_store_root, unlike resolver_config in lang/tooling/fol-frontend/src/compile/mod.rs:1564-1588. Verified empirically in one directory: `folc code check` (workspace route) loads the dep from .fol/pkg, while `folc code check src/main.fol` (direct route) fails with R1001 'resolver pkg import ... requires an explicit package store root'. Same program, contradictory results depending on how the path is spelled.

### 7. shell() child process competes with the stdin key-feed thread for input
- **file**: `lang/execution/fol-runtime/src/std.rs`
- **evidence**: key_feed (std.rs:356-373) uses a OnceLock to lazily spawn a thread that takes std::io::stdin() and loops `while let Ok(1) = stdin.read(&mut buffer)` forever, with no stop/pause channel; it is initialized on the first read_key (377-385) or read_key_ms (390+). shell (std.rs:797-806) is documented at :798 as "The TUI suspend/exec primitive" and runs `sh -c <cmd>` via .status(), inheriting fd 0. After any read_key call, the feed thread and the interactive child both read the same fd, so bytes intended for the child are stolen nondeterministically — the suspend-to-editor/pager flow the primitive exists for. No mechanism exists to quiesce the feed.

### 8. make test runs the two 'ignored' real-GitHub network tests in CI
- **file**: `Makefile`
- **category**: network-dependence
- **evidence**: Makefile:108-110 defines `test:` as `cargo test --workspace $(TEST_ARGS)` followed by `cargo test -- $(TEST_ARGS) --ignored`, and Makefile:114 `verify: fmt-check lint test interop-check test-interop`, which tests.yml:61 runs (`run: make verify`). The un-ignored tests are real: test/integration_tests/integration_editor_and_build.rs:7638-7668 test_frontend_fetches_public_logtiny_from_github hits git+https://github.com/bresilla/logtiny.git with tag:v0.1.4 and hash 7b8fe6b033e4, and :7670-7719 test_frontend_fetches_public_logtiny_version_matrix_from_github hits branch:develop, tag:v0.1.4 and commit:7b8fe6b033e4ea1f1d5337d5f141353597db7a07. They are reachable from the root package's integration target (Cargo.toml:37-39 [[test]] name="integration" path="test/run_tests.rs"; run_tests.rs:508 includes the file). Hermetic equivalents confirmed at :5237 (test_std_logtiny_git_example_builds_against_local_git_remote) and :5312 (selector matrix), both using create_git_remote_from_logtiny_fixture + git+file:// locators. Extra sloppiness: the ignore reason at :7639 says "exercises git fetch through a local fixture remote" while the test fetches GitHub.
- **detail**: Makefile:110 runs 'cargo test -- $(TEST_ARGS) --ignored' as part of 'make test' (and thus 'make verify', which tests.yml executes). This un-ignores test_frontend_fetches_public_logtiny_from_github (test/integration_tests/integration_editor_and_build.rs:7638-7666) and test_frontend_fetches_public_logtiny_version_matrix_from_github (:7670-7714), both of which git-fetch https://github.com/bresilla/logtiny.git. CI and local 'make test' therefore hard-depend on github.com availability and on that external repo keeping branch 'develop', tag v0.1.4 and commit 7b8fe6b033e4... alive. Offline 'make test' always fails. Hermetic equivalents already exist (test_std_logtiny_git_example_builds_against_local_git_remote at :5237 and the selector matrix at :5310 use git+file:// fixture remotes built from test/fixtures/logtiny).

### 9. CI never runs on main: merges and release tags are untested
- **file**: `.github/workflows/tests.yml`
- **category**: ci-coverage
- **evidence**: tests.yml:3-7 is `on: push: branches: ["develop"] / pull_request: branches: ["develop"]` only. `git symbolic-ref refs/remotes/origin/HEAD` returns refs/remotes/origin/main, and main carries real work (6088f8c0 "chore: merge feature_v2_redo to main", b94eba79 at origin/HEAD). release.yml:11 defines exactly one job (`toolchain:`) with no test/verify step — grep of top-level job keys confirms nothing else — so a v* tag (release.yml:4-5) publishes user-facing assets from a tree CI never verified. Neither pushes nor PRs to main run make verify.
- **detail**: tests.yml lines 4-7 trigger only on push/PR to 'develop'. The repo's default branch is main (origin/HEAD -> main) and real work lands there (6088f8c0 'chore: merge feature_v2_redo to main'); release tags (v0.2.1, v0.2.2) are cut from that line. Nothing runs `make verify` on pushes or PRs to main, and release.yml has no test job either, so a tag can ship a completely untested tree. With the branch model now develop/main only, tests.yml needs both branches (push: [develop, main], pull_request: [develop, main]).

### 10. `fol self install` writes toolchains outside FOL_HOME and remove_dir_all's arbitrary paths (proven)
- **file**: `lang/tooling/fol-self/src/main.rs:502-508`
- **category**: security
- **evidence**: Re-proven live: `FOL_HOME=$S/folhome fol self install '0.9.9/../../../../escaped' --from <repo>` exited 0, printed 'installed fol 0.9.9/../../../../escaped -> .../folhome/toolchains/v0.9.9/../../../../escaped', and folc+std+runtime landed in a directory OUTSIDE FOL_HOME (verified on disk). looks_like_version (main.rs:225-228) checks only the first char is an ASCII digit; normalize_version_dir (220-223) keeps the '../' segments; main.rs:503-505 runs fs::remove_dir_all on the traversed path before copying, so a crafted spec deletes arbitrary directories. The same unvalidated spec reaches install_from_network via resolve_toolchain's auto-fetch (316-320), fed by the repo-controlled //fol pin (parse_toolchain_pin, 184-203).
- **detail**: looks_like_version only checks that the first char (after an optional 'v') is an ASCII digit, so '0.9.9/../../../../escaped' passes. normalize_version_dir then yields 'v0.9.9/../../../../escaped'; create_dir_all materialises the 'v0.9.9' component and the `..` segments resolve past FOL_HOME. PROVEN: $ fol self install '0.9.9/../../../../escaped' --from /repo installed fol 0.9.9/../../../../escaped -> $FOL_HOME/toolchains/v0.9.9/../../../../escaped (exit 0) -> folc + std/ + runtime/ written into <scratch>/escaped, outside FOL_HOME. Worse, line 504 runs remove_dir_all on the same traversed path first, so `fol self install '0.2.0/../../../../home/me/work' --from .` destroys that directory. The same unvalidated spec reaches install_from_network (line 534) — and that path is reachable without any `fol self` command at all, because resolve_toolchain auto-installs whatever a project's `//fol` pin says (lines 316-320).

### 11. Downloaded toolchains are never verified, and a repo-controlled pin triggers the download
- **file**: `lang/tooling/fol-self/src/main.rs:531-591`
- **category**: security
- **evidence**: install_from_network (531-570): fetch() runs curl with only '-fL --silent --show-error' (no --max-time/--connect-timeout) or wget -q, then `tar -xzf` with no checksum, signature, size limit, or --no-same-owner/--no-same-permissions flags (551-556). RELEASE_URL_BASE is a hardcoded const (line 15) with no override. Grep confirms fol-self reads neither FOL_OFFLINE nor FOL_LOCKED (those exist only in fol-frontend/src/config.rs:113-118). dispatch() passes allow_fetch=true for any non-help command (394), and select_toolchain_spec (354-355) takes the spec from the //fol pin of a cloned repo's build.fol — so `cd untrusted-repo && fol code build` performs an unauthenticated download+extract chosen by that repo.
- **detail**: install_from_network curl/wget's a tarball over the network and unpacks it with `tar -xzf` with no checksum, no signature, no size limit, no --no-same-owner/--no-same-permissions, and no connect/transfer timeout (so a stalled server hangs `fol` forever with `--silent` and zero output). RELEASE_URL_BASE is hardcoded with no override, so there is no way to point at a mirror or to test the flow offline. The trigger is not an explicit user command: resolve_toolchain (316-320) auto-fetches whenever the selected spec merely looks version-shaped, and the spec can come from the `//fol` line of a build.fol you just cloned. So `cd untrusted-repo && fol code build` performs an unauthenticated download+extract chosen by that repo. The frontend's FOL_OFFLINE / FOL_LOCKED knobs (lang/tooling/fol-frontend/src/config.rs:117-121) are not honoured by the manager, so there is no way to forbid this.

### 12. FOL_HOME/pkg 'shared package store' is documented but no code ever uses it
- **file**: `book/src/025_toolchain/_index.md`
- **category**: docs-truth
- **evidence**: book/src/025_toolchain/_index.md:42 ('pkg/ <- third-party packages, shared by all versions'), 200_versions.md:17-19 ('The shared parts of the home — pkg/ for third-party packages ... version-independent by design'), README.md:46 all describe the shared store. Reality: fol-self's ensure_home_layout (main.rs:138) creates the empty dir and nothing else in the workspace touches it; grep for FOL_HOME across all .rs files hits only fol-self plus comments; every actual store-root chain resolves to project-local .fol/pkg or the bundled store (fetch.rs:50-59, build_route/mod.rs:323-327, compile/mod.rs:1346-1353 and 1570-1577). The documented capability does not exist. Kept high per the repo's own docs-truth rule (std README: 'Anything outside that list should not be documented as already shipped').
- **detail**: README.md:46 ('pkg/ <- packages, shared by all versions'), book/src/025_toolchain/_index.md:42-43, and book/src/025_toolchain/200_versions.md:17-19 ('The shared parts of the home -- pkg/ for third-party packages') describe $FOL_HOME/pkg as the version-shared third-party package store. In reality: fol-self (lang/tooling/fol-self/src/main.rs:137-143, ensure_home_layout) merely creates the empty directory, and nothing ever reads or writes it -- folc never reads FOL_HOME at all (zero hits outside comments), and the actual fetch/package store is project-local <project>/.fol/pkg (lang/tooling/fol-frontend/src/fetch.rs:58, lang/tooling/fol-frontend/src/build_route/mod.rs:327, lang/tooling/fol-frontend/src/compile/mod.rs:1346) or the bundled store next to the binary. The documented capability is fiction and the created dir is a dead path.

### 13. Compiler panics on select arm waiting on a first-class chn[rx,T] receiver value
- **file**: `lang/compiler/fol-lower/src/exprs/flow.rs`
- **category**: panic-reachable-from-user-input
- **evidence**: Reproduced live with target/release/folc (built at last-commit time): `var receiver: chn[rx, int] = channel[rx]; select { when receiver as value {...} * {...} }` under `folc code check` -> "thread 'main' panicked at lang/compiler/fol-lower/src/exprs/flow.rs:903:13: internal error: entered unreachable code", exit 101. Cause verified: typecheck accepts it (fol-typecheck/src/exprs/controlflow.rs:531 calls channel_receiver_element_type, which at helpers.rs:717-718 accepts both Channel and ChannelReceiver), but the select lowering at flow.rs:901-903 matches only LoweredType::Channel and unreachable!s. A twin unreachable! sits at flow.rs:709 (iteration path).
- **detail**: PROBE-CONFIRMED. Same typecheck/lower parity break as the iteration panic: select-arm typing goes through `channel_receiver_element_type` (fol-typecheck/src/exprs/controlflow.rs:531) which accepts ChannelReceiver, but the select lowering at fol-lower/src/exprs/flow.rs:901-903 only matches `LoweredType::Channel` and `unreachable!`s otherwise. Repro: `var receiver: chn[rx, int] = channel[rx]; select { when receiver as value { ... } * { ... } };` -> `panicked at lang/compiler/fol-lower/src/exprs/flow.rs:903:13` (exit 101) from plain `folc code check`. Book line 92 promises 'blocking receive, receiver iteration, or selected arm' all transfer payloads, so selected-arm-over-receiver is a promised surface. The direct receive path (`receiver[rx]` as an expression, lower_channel_access) handles ChannelReceiver correctly — only these two flow.rs sites were never updated when the chn[rx,T] endpoint landed.

### 14. Direct-file `code run` silently discards the program's stdout
- **file**: `lang/tooling/fol-frontend/src/direct.rs`
- **category**: bug
- **evidence**: Reproduced live: main.fol with `.echo("hello-direct")` — `folc code run main.fol` exits 0 printing only the 'Done: ran <binary>' summary; running the produced binary directly prints 'hello-direct'. Code confirms: direct.rs Run branch spawns with .output() (line 343), stdout is never written anywhere, stderr only on failure (349-355). compile/mod.rs:10-22 has forward_child_output doing this correctly for the workspace route; direct.rs never calls it.
- **detail**: In the DirectCompileMode::Run branch (lang/tooling/fol-frontend/src/direct.rs:336-374) the child is spawned with .output() at line 343, but output.stdout is never forwarded anywhere — on success nothing is printed (stderr only on failure, line 349-355). Verified empirically: `folc code run main.fol` with a program calling .echo("hello-direct") exits 0 and prints only the 'ran <binary>' summary; 'hello-direct' never appears. The workspace route does this correctly via forward_child_output (lang/tooling/fol-frontend/src/compile/mod.rs:10-22, called at 899 and 986). Fix: forward output.stdout/output.stderr in direct.rs like compile/mod.rs does.

### 15. No CI test run ever executes on the main branch
- **file**: `.github/workflows/tests.yml`
- **category**: ci-gap
- **evidence**: tests.yml:4-7: push/pull_request restricted to ["develop"]. book.yml:4-6: develop-only (+workflow_dispatch). release.yml:3-5 triggers on v* tags and its job list contains zero test steps (only checkouts, cargo build --release, stage/pack). origin HEAD is refs/heads/main and recent release commits (b94eba79, 5dd4e487, 6088f8c0) landed on main — so main is never gated and a broken main can be tagged and released untested.
- **detail**: tests.yml lines 4-7 trigger only on push/PR to 'develop'. book.yml (lines 3-7) is also develop-only, and release.yml triggers only on v* tags and runs zero tests. The repo's default branch is 'main' and recent commits (b94eba79, 5dd4e487, 6088f8c0) landed directly on main — meaning merges to main, including the whole binary-split release line, are never gated by CI. A broken main can be tagged and released untested.


## CONFIRMED — MEDIUM (53)

### 1. Dead compat aliases Diagnostic::legacy_location()/legacy_help()
- **file**: `lang/compiler/fol-diagnostics/src/model.rs`
- **category**: dead-code
- **evidence**: model.rs:222-228 defines legacy_location() and legacy_help() as pure pass-throughs to primary_location()/first_help(). Workspace-wide grep shows the only callers are fol-diagnostics' own tests at lang/compiler/fol-diagnostics/src/lib.rs:345 (test named test_coded_report_helpers_preserve_legacy_location_and_help_views), :365, :373, :477. Named 'legacy' compatibility views with zero production callers in a greenfield repo.
- **detail**: model.rs:222-228 defines legacy_location() and legacy_help() as pure pass-throughs to primary_location()/first_help(). Their only callers are fol-diagnostics' own tests (lang/compiler/fol-diagnostics/src/lib.rs:345, 365, 373, 477). They are literally named 'legacy' compatibility views in a greenfield repo — delete the aliases and point the tests at the real accessors.

### 2. Broken/stale frontend overview page: transition narrative plus orphaned bullet list
- **file**: `book/src/000_overview/200_frontend.md`
- **category**: stale-docs
- **evidence**: Line 3: "FOL now has a dedicated frontend layer"; line 5: "That layer is the `fol` tool itself" (false post-split: `fol` is the fol-self manager, the frontend crate is the `folc` engine); line 21 "Use this overview page only as the entrypoint pointer." is immediately followed by a lead-in-less bullet list at lines 23-27 ("- workflow commands ... - frontend diagnostics") and the dangling line 29 "So the root binary is no longer its own separate CLI implementation." — clearly botched editing debris plus forbidden now-has/no-longer transition prose.
- **detail**: Line 3 "FOL now has a dedicated frontend layer" and line 29 "So the root binary is no longer its own separate CLI implementation" are change-log-style transition prose (forbidden 'now has'/'no longer' narrative in a greenfield spec). Worse, lines 23-27 are an orphaned bullet list ('- workflow commands ... - frontend diagnostics') dangling after line 21's "Use this overview page only as the entrypoint pointer." with no lead-in sentence — clearly a botched edit. Line 5/7 also repeat the pre-split claim that the `fol` tool is fol-frontend.

### 3. Macros chapter cites removed memory sigils '#', '&', '*' as compiler-owned
- **file**: `book/src/300_meta/200_macros.md`
- **category**: stale-docs
- **evidence**: Lines 10-12 state macros "will not be able to redefine the compiler-owned memory sigils `@`, `#`, `!`, `&`, or `*`; those spellings already have fixed ownership, borrowing, and pointer semantics." The `#x`/`&x`/`*x` sigils were removed from the surface (replaced by option spellings [bor]/[ref]/[drf]); SYMBOL::Hash is not even consumed by any parser anymore (see the lexer finding). The 'already have fixed semantics' claim is false for the current language.
- **detail**: Lines 10-12: macros "will not be able to redefine the compiler-owned memory sigils `@`, `#`, `!`, `&`, or `*`; those spellings already have fixed ownership, borrowing, and pointer semantics." `#x`, `&x`, `*x` were removed from the surface (replaced by [bor]/[ref]/[drf] option spellings); the claim that they 'already have fixed semantics' is false for the current language.

### 4. docs/ is gitignored yet 8 docs files are tracked; v2-* milestone notes are self-described historical junk
- **file**: `.gitignore`
- **category**: stale-docs
- **evidence**: .gitignore line 5 is `docs/`, yet `git ls-files docs/` returns 8 tracked files (bundled-std, editor-sync, runtime-models, v2-full-contract, v2-generics-m1, v2-quality-gates, v2-runtime-strategy, v2-standards-m2). docs/v2-generics-m1.md:3 and docs/v2-standards-m2.md:3 both open with "Historical transition note:". docs/bundled-std.md's resolution order claims step 2 "the project-local fetched store `.fol/pkg`" for std, but lang/compiler/fol-package/src/config.rs (available_bundled_std_root, lines ~18-30) only checks exe-sibling std/ then the compiled-in source-tree path — no .fol/pkg step for std.
- **detail**: .gitignore line 5 ignores docs/ but git tracks docs/{bundled-std,editor-sync,runtime-models,v2-full-contract,v2-generics-m1,v2-quality-gates,v2-runtime-strategy,v2-standards-m2}.md — edits stay versioned while any new doc is silently ignored. docs/v2-generics-m1.md:3 and docs/v2-standards-m2.md:3 open with "Historical transition note" — V2 milestone-tracking notes that are historical junk under the greenfield rule. Additionally docs/bundled-std.md:114-120 claims std-root resolution order includes "the project-local fetched store `.fol/pkg`" as step 2; the actual resolver (lang/compiler/fol-package/src/config.rs:24-30) only checks exe-sibling std/ then the compiled-in source-tree path — the .fol/pkg step applies to the package store, not std. Decide: un-ignore docs/ and keep only current-truth contracts, or delete the historical notes.

### 5. `fol self link` writes its manifest at an attacker/typo-controlled path; name is never validated
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: self_link (main.rs:593-616): the only name check is looks_like_version at :597 (rejects names whose first char after optional 'v' is an ASCII digit). `'../../pwned'` and `''` both pass, and :611 does toolchains_root(home).join(format!("{name}.toml")) — so '../../pwned.toml' is written two levels above FOL_HOME/toolchains and '' writes the hidden 'toolchains/.toml', which installed_toolchains (:337-339) then lists as a nameless entry and resolve_linked (:246) resolves. Real input-validation bug; downgraded from high to medium because the name comes only from the local user's own CLI argument (no trust boundary crossed).

### 6. read_default_toolchain aborts the whole config scan on any line starting with "default"
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: main.rs:147-159: inside the for loop, line 152 is `let value = value.trim_start().strip_prefix('=')?.trim();`. For a config line like `defaults = whatever`, strip_prefix("default") yields Some("s = whatever"), strip_prefix('=') yields None, and `?` returns None from the entire function instead of continuing the loop — so a later valid `default = 0.2.0` line is never read. Masked only when exactly one toolchain is installed (single-toolchain fallback at :360-363). Also confirmed: no '#' comment handling here while parse_link_manifest (:230-243) has it — two ad-hoc parsers for sibling files.

### 7. Post-extract validation only checks folc, and the broken toolchain is left behind
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: install_from_network: main.rs:563-567 checks only `destination.join("folc").is_file()`; std/ and runtime/ are never verified even though release.yml (comment at ~line 71-72: "Archive root must hold exactly {folc, std/, runtime/}") and install_from_source (:511-522, which copies std and runtime) establish the three-part contract. On failure it returns Err leaving the half-extracted directory in place and tells the user "removing it is safe: fol self remove {version}" instead of removing it.

### 8. `fol +<toolchain> self ...` is impossible and dead-ends in the frontend's redirect
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: run() (main.rs:23-28) matches "self"/"s" only at args[0]; a leading +spec falls to dispatch(), which strips the +spec (:380-383) and execs folc with `self ...`, and fol-frontend/src/cli/parser.rs:520-524 rejects that with "toolchain management lives in the `fol` manager binary\nrun: fol self <command>". Meanwhile the manager's own fallback help advertises `fol [+<toolchain>] [COMMAND]` (main.rs:59) with `self` listed as a COMMAND (:66), so the documented surface promises a combination that always errors.

### 9. Removing the default toolchain leaves a dangling default and can silently re-download it
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: self_remove (main.rs:659-678) deletes the link manifest or toolchain dir but never reads or updates $FOL_HOME/config. After removing the configured default, select_toolchain_spec still returns it (:357-359); dispatch calls resolve_toolchain with allow_fetch = !help_like (:394), and resolve_toolchain (:316-320) silently re-fetches any version-shaped spec (`eprintln!("toolchain {spec} not installed, fetching...")` then install_from_network) — re-installing exactly what the user just deleted. Name-shaped defaults instead error on every command.

### 10. docs/bundled-std.md 'Bootstrap Surface' badly stale: omits std.term/std.strn/std.time and half of std.io
- **file**: `docs/bundled-std.md`
- **category**: stale-docs
- **evidence**: The 'Bootstrap Surface' section (~lines 129-149) lists only std.fmt/std.fmt.math/std.io and 9 routines, and the identical routine list is duplicated verbatim under 'Current shipped public routines' (~lines 185-195). Actual shipped std (lang/library/std): root lib.fol exports env/shell/dir_list/read_file; io/lib.fol has 7 exports (echo_int/echo_str/echo_bool/echo_chr/write/read_key/read_key_ms); term/lib.fol (raw_mode/cols/rows), strn/lib.fol (sub/byte_at/from_byte), time/lib.fol (sleep_ms/now_ms), fmt int_to_str — none documented. Downgraded high→medium: severe staleness, but in an internal (gitignored-dir) doc, not the book.
- **detail**: Lines 133-149 list the public modules as only std.fmt, std.fmt.math, std.io and exactly 9 routines; lines 186-196 repeat the identical stale list verbatim (sloppy duplication). Actual shipped std (lang/library/std): root lib.fol exports env, shell, dir_list, read_file; io/lib.fol has 7 exports (adds write, read_key, read_key_ms); plus whole undocumented modules term/lib.fol (raw_mode, cols, rows), strn/lib.fol (3 exports), time/lib.fol (2 exports). The doc's own rule at line 6 ('only actually shipped public names should be documented as available') is violated by the doc itself.

### 11. Overview 'Frontend Workflow' page stale post-split with dangling leftover prose
- **file**: `book/src/000_overview/200_frontend.md`
- **category**: stale-docs
- **evidence**: Duplicate of the other 200_frontend.md finding, and equally real: lines 3-7 claim the `fol` tool is the fol-frontend layer (stale — `fol` is now the fol-self manager, fol-frontend implements the `folc` engine per ARCHITECTURE.md:82-90), and lines 21-29 are editing debris: an orphaned 5-item bullet list with no lead-in followed by the detached sentence "So the root binary is no longer its own separate CLI implementation."
- **detail**: Lines 3-7: 'That layer is the `fol` tool itself. The frontend is implemented in `fol-frontend`' -- stale; `fol` is now the fol-self manager and fol-frontend is `folc`. Lines 21-29 are editing debris: 'Use this overview page only as the entrypoint pointer.' is immediately followed by a disconnected bullet list ('workflow commands / direct compile dispatch / root help / output rendering / frontend diagnostics') and the orphaned sentence 'So the root binary is no longer its own separate CLI implementation.' that no longer attaches to anything.

### 12. Misleading unreachable! contract on channel_binding_local across five call sites
- **file**: `lang/compiler/fol-lower/src/exprs/expressions.rs`
- **category**: hardening
- **evidence**: channel_binding_local (expressions.rs:84-95) accepts Channel|ChannelSender|ChannelReceiver. Callers asserting `unreachable!("channel_binding_local verifies the lowered type")` with narrower assumptions: expressions.rs:114 (matches all three — actually exhaustive), expressions.rs:207 (lower_channel_send matches only Channel|ChannelSender; ChannelReceiver hits unreachable!, guarded only by a typecheck rejection in another crate), flow.rs:709 and flow.rs:903 (both let-else on exactly LoweredType::Channel, so any endpoint value reaching them aborts the process). The duplicated `unreachable!("channel-send guard preserves the endpoint shape")` confirmed at fol-typecheck/src/exprs/operators.rs:53, fol-lower/src/exprs/body.rs:950, fol-lower/src/exprs/expressions.rs:767. The surrounding code's own convention is Err(LoweringError::with_kind(InvalidInput, ...)) for invariant misses (e.g. expressions.rs:90-94, :116-120).
- **detail**: Five call sites assert `unreachable!("channel_binding_local verifies the lowered type")` but the claim is false: channel_binding_local (expressions.rs:84-95) verifies only that the type is one of Channel/ChannelSender/ChannelReceiver, while each caller assumes a narrower subset. Sites: expressions.rs:114 (actually exhaustive, safe), expressions.rs:207 (lower_channel_send matches only Channel|ChannelSender; a receiver in send position is guarded solely by a typecheck rejection in a different crate — probe shows typecheck does reject today, T1002), flow.rs:709 and flow.rs:903 (both are the confirmed live panics above). These should be `LoweringError::with_kind(InvalidInput, ...)` returns, not process aborts — the compiler's own convention everywhere else in these functions is to return Err for invariant misses. Same pattern with the same fragility: `unreachable!("channel-send guard preserves the endpoint shape")` duplicated at fol-typecheck/src/exprs/operators.rs:53, fol-lower/src/exprs/body.rs:950, and fol-lower/src/exprs/expressions.rs:767.

### 13. SYMBOL::Hash ('#') still lexed but no parser consumes it — leftover of removed # borrow sigil
- **file**: `lang/compiler/fol-lexer/src/lexer/stage1/element.rs`
- **category**: legacy-leftover
- **evidence**: element.rs:393 `'#' => self.key = Symbol(SYMBOL::Hash)`; variant at lang/compiler/fol-lexer/src/token/symbol/mod.rs:32 with display "#" at :68. grep for SYMBOL::Hash across lang/ and test/ finds zero matches outside fol-lexer — no parser, typecheck, or lower code consumes it. Two comments still explain semantics by reference to the removed sigil: fol-lower/src/exprs/expressions.rs:481 and fol-typecheck/src/exprs/bindings.rs:482, both "exactly like the legacy `#owner`" — direct violations of the no-reference-to-replaced-syntax rule.
- **detail**: The lexer still produces a dedicated token for '#' (stage1/element.rs:393 `'#' => self.key = Symbol(SYMBOL::Hash)`, variant declared at token/symbol/mod.rs:32 with display at :68), but grep shows no code outside fol-lexer ever matches SYMBOL::Hash — the `#owner` borrow sigil was removed from the language (replaced by [bor]). The token is a dead remnant of the deleted surface; per the no-legacy rule it should be dropped (letting '#' fall into the Illegal/unknown-character path like any other unsupported character), and the two comments that still explain semantics 'exactly like the legacy `#owner`' (fol-lower/src/exprs/expressions.rs:481, fol-typecheck/src/exprs/bindings.rs:482) should be rewritten without reference to removed syntax.

### 14. Dependency-load errors truncated to first error and stripped of import context (the bogus-R1003 issue)
- **file**: `lang/compiler/fol-resolver/src/session/mod.rs`
- **category**: diagnostics-quality
- **evidence**: session/mod.rs:204-217: load closure maps resolve_parsed_package's Vec of errors through `errors.into_iter().next().unwrap_or_else(...)` (line 208), discarding all but the first dependency error. imports.rs:234-249 import_error_from returns the error unchanged whenever `error.origin().is_some()`, attaching the importing-alias origin only to origin-less errors — so an in-dependency resolution error surfaces pointing into the dependency's own file with no note about the `use` site. Downgraded high→medium: compile still fails with a real (if incomplete and context-poor) diagnostic; this is diagnostics degradation, not wrong behavior.
- **detail**: load_prepared_package (lang/compiler/fol-resolver/src/session/mod.rs:204-217) collapses a failed dependency resolution to `errors.into_iter().next()` (line 208), silently dropping every error after the first. Additionally import_error_from (lang/compiler/fol-resolver/src/imports.rs:234-249) attaches the importing alias origin only when the error has NO origin, so a resolution error from inside the dependency surfaces as a bare R1003 pointing into the dependency's file with zero indication it happened while loading a dependency. Verified empirically: a store dep with two unresolved names (`missing_name`, `missing_too`) produces exactly one R1003 for 'missing_name' located in .fol/pkg/dep/src/lib.fol, no mention of the `use dep: pkg` site in src/main.fol and no second error. Fix: propagate all errors (change load path to Vec<ResolverError>) and add a related_origin/note 'while loading dependency <alias> imported at <file:line>'.

### 15. LSP entrypoint discards FrontendConfig entirely
- **file**: `lang/tooling/fol-frontend/src/editor.rs`
- **category**: hardening-gap
- **evidence**: editor.rs:41-44: `pub fn editor_lsp_stdio(config: &FrontendConfig) -> FrontendResult<()> { let _ = config; fol_editor::run_lsp_stdio(fol_editor::EditorConfig::default()).map_err(lower_editor_error) }`. The config parameter is explicitly swallowed with `let _ = config;` and the editor session always starts with EditorConfig::default(), so any --std-root/--package-store-root/cwd configuration passed to `fol tool lsp` never reaches the LSP.
- **detail**: editor_lsp_stdio (lang/tooling/fol-frontend/src/editor.rs:40-43) does `let _ = config;` and starts the LSP with fol_editor::EditorConfig::default(). Any --std-root / --package-store-root / working-directory configuration the user passes to `fol tool lsp` never reaches the editor session, compounding the LSP-side store-resolution drift (analysis.rs:228).

### 16. Editor analysis overlay temp directory leaked on every error path
- **file**: `lang/tooling/fol-editor/src/workspace.rs`
- **category**: temp-cleanup
- **evidence**: materialize_analysis_overlay creates temp_root /tmp-side fol_editor_overlay_* at workspace.rs:162-180 (fs::create_dir_all at :172), then has multiple `?`/ok_or_else early returns before the EditorAnalysisOverlay is constructed at ~:255: copy_directory_tree(:182), relative_overlay_path ok_or_else(:184-193), fs::create_dir_all parent(:197), fs::write of the overlay document(:207), and materialize_active_internal_standard_aliases(:249). Cleanup exists only in `impl Drop for EditorAnalysisOverlay` (workspace.rs:73-77, fs::remove_dir_all(&self.temp_root)), which never runs on those paths — each failure leaks a full copied workspace tree in the system temp dir.
- **detail**: materialize_analysis_overlay creates /tmp/fol_editor_overlay_* (lang/tooling/fol-editor/src/workspace.rs:162-181) and then copies the entire workspace tree into it (line 182), but cleanup happens only via Drop on the returned EditorAnalysisOverlay (lines 73-75). Every `?` return between creation (line 172) and construction (line 255) — copy_directory_tree failure, relative_overlay_path failure, fs::write failure, materialize_active_internal_standard_aliases failure — leaks a full workspace copy in the system temp dir. Wrap the temp root in a guard that removes it unless ownership is transferred to the overlay.

### 17. OS/fs wrappers dumped at std package root; module split not principled
- **file**: `lang/library/std/lib.fol`
- **evidence**: lib.fol:1-15 exports env, shell, dir_list, read_file at the std package root while all other wrappers are namespaced (io/, term/, strn/, time/, fmt/). lang/library/std/README.md:83 says "`std.os` is still deferred until it has one honest user-facing API" and docs/bundled-std.md:172 says "no fake placeholder `std.os` module yet" — both deny an OS surface that in fact ships, just unnamespaced at the root. Placement inconsistency also confirmed: int->str in fmt (fmt/root.fol:19-21) vs byte->str in strn (strn/lib.fol from_byte); intrinsic env_var renamed to 'env' while shell/dir_list/read_file keep intrinsic names.

### 18. Failure-blind error model: read_file/env_var/dir_list return empty string, shell conflates -1
- **file**: `lang/execution/fol-runtime/src/std.rs`
- **evidence**: std.rs:791-795 env_var: `.unwrap_or_else(|_| FolStr::new(""))` (unset == set-to-empty); :798-806 shell: `.map(|status| status.code().unwrap_or(-1) ...).unwrap_or(-1)` (cannot-start == killed-by-signal == exit -1); :809-827 dir_list returns "" via unwrap_or_default for unreadable dirs; :829-832 read_file: `read_to_string(...).unwrap_or_default()` (missing/unreadable == empty file). All four doc comments admit the sentinel behavior. The language's own error story is opt[T]/err[T] shells, so FOL code cannot distinguish failure from empty over this entire OS family — a real hardening gap even though documented.

### 19. Published book is stale: book.yml deploys only from develop and gh-pages is 826 book lines behind HEAD
- **file**: `.github/workflows/book.yml`
- **category**: ci-coverage
- **evidence**: book.yml:4-6 triggers only on push to develop (+workflow_dispatch). origin/gh-pages tip commit is 'deploy: 42473f68...' while origin/main and origin/develop are both at b94eba79; `git diff --stat 42473f68..HEAD -- book/` = 25 files, 826 insertions (including the toolchain-management chapter). The mdbook note is also empirically CONFIRMED: with mdbook v0.5.3 installed locally, `mdbook build <bookdir> --dest-dir docs` run from a parent directory writes ./docs relative to CWD, so book.yml:28's `mdbook build book --dest-dir docs` at repo root writes into the git-tracked docs/ directory (which also explains how gh-pages got the rendered book despite publish_dir ./docs).
- **detail**: book.yml:4-6 triggers only on push to develop. gh-pages was last deployed from 42473f68 (2026-07-16), while origin/main and origin/develop both sit at b94eba79 (post-v0.2.2) with `git diff --stat 42473f68..HEAD -- book/` showing 826 insertions across 25 files — including the new toolchain-management guide (9d413052). The recent work landed via a merge to main, which the develop-only trigger never fires on. Either add main to the trigger (releases/docs flow through main now) or investigate why the develop push at b94eba79 did not redeploy (possible failed run under the newly pinned mdbook 0.5.3). Note line 28 `mdbook build book --dest-dir docs` writes into the repo-root docs/ directory, which is tracked (see .gitignore finding) — running that exact command locally clobbers tracked files.

### 20. cliff.toml silently drops hundreds of real commits from the changelog; ^chore|ci regex unanchored
- **file**: `cliff.toml`
- **category**: release-hygiene
- **evidence**: cliff.toml:34 `filter_unconventional = true`; :54 `{ message = "^chore|ci", ... }` — the alternation binds as (^chore)|(ci), matching 'ci' anywhere in any subject; :61 `filter_commits = false`; no ^ci or ^build parsers exist (:43-56). The v0.1.4..v0.2.1 range has 3209 commits, 238 of them with bare-scope non-conventional subjects like 'tests:'/'frontend:' (including the real change 'frontend: remove compatibility build routing', verified via git log), all silently dropped by filter_unconventional. Bonus corroborating neglect: cliff.toml:26's <REPO> postprocessor replaces with 'https://github.com/bresilla/roc' — a different project's URL, so any (#N) issue links in the changelog point at the wrong repo.
- **detail**: cliff.toml:34 `filter_unconventional = true` combined with the repo's history of bare-scope subjects ('tests:', 'typecheck:', 'package:', 'frontend:' — not conventional-commit types) means git-cliff skipped ~771 of the 3209 commits in the v0.1.4..v0.2.1 range (the run-time 'skipped N commits' warnings), so CHANGELOG.md for 0.2.1 is materially incomplete — those skips include real changes like 'frontend: remove compatibility build routing'. Additionally cliff.toml:54 `{ message = "^chore|ci" }` is an unanchored alternation: any not-yet-matched subject containing the substring 'ci' anywhere is grouped as Miscellaneous, and there are no ^ci/^build parsers at all while filter_commits=false leaves such commits ungrouped. Either accept conventional-only changelogs going forward (history is fixed) or set filter_unconventional=false for the historical ranges; fix the regex to `^(chore|ci)`.

### 21. Doc comments claim removed '#'/'@' ownership forms still coexist 'until retired'
- **file**: `lang/compiler/fol-typecheck/src/exprs/operators.rs`
- **category**: legacy-leftover
- **evidence**: operators.rs:574-578 reads verbatim: "wired additively alongside the legacy `#`/`@`/implicit forms ... so the two spellings stay semantically identical until the legacy forms are retired." The `#` form is dead: SYMBOL::Hash is lexed at lang/compiler/fol-lexer/src/lexer/stage1/element.rs:393 and rendered at token/symbol/mod.rs:68, and grep over all of lang/ finds NO other SYMBOL::Hash use — no parser rule consumes it, so there is no `#owner` surface to stay parity with. Stale phrasing also present verbatim at fol-typecheck/src/exprs/bindings.rs:481-482 ("exactly like the legacy `#owner`") and fol-lower/src/exprs/expressions.rs:480-481 (same). Migration/coexistence posture is exactly what the greenfield no-legacy rule forbids.
- **detail**: operators.rs:574-578 doc comment on type_ownership_op says the explicit [opt,...] ownership ops are "wired additively alongside the legacy `#`/`@`/implicit forms: each option combination reuses the same checked machinery the legacy forms do, so the two spellings stay semantically identical until the legacy forms are retired." The raw `#` borrow sigil was deleted (only [bor] exists); there is no coexistence and nothing left to retire. Same stale phrasing at lang/compiler/fol-typecheck/src/exprs/bindings.rs:482 and lang/compiler/fol-lower/src/exprs/expressions.rs:481 ("exactly like the legacy `#owner`").

### 22. Resolver keeps a self-described 'legacy single-program surface' (4 public APIs)
- **file**: `lang/compiler/fol-resolver/src/lib.rs`
- **category**: legacy-leftover
- **evidence**: lib.rs:58, 70, 98, 108 each say "This is the legacy single-program surface"; line 59-60 adds "New semantic consumers should prefer the workspace APIs". Mirrored free functions at lib.rs:136-174. Callers are only fixtures/tests: fol-typecheck/src/lib.rs:111,126,349, model.rs:1424,1458,1497, exprs/mod.rs:2861, test/typecheck/test_typecheck.rs:25,80,106,127, test/resolver/test_resolver.rs:18,46, test/integration_tests/integration_pipeline.rs:198,242. Also confirmed the shipped user-facing message at fol-typecheck/src/exprs/calls.rs:3004: "imported symbol '{}' requires workspace-aware typechecking in V1; the legacy single-package path is not sufficient" — a diagnostic naming 'V1' and a 'legacy path'.
- **detail**: lib.rs:58, 70, 98, 108 document resolve_package, resolve_package_with_config, resolve_prepared_package, resolve_prepared_package_with_config as "the legacy single-program surface" with "new semantic consumers should prefer the workspace APIs". These are still widely used by tests (test/typecheck/test_typecheck.rs:3,25; test/resolver/test_resolver.rs:4,18; typecheck in-crate fixtures). Related user-facing leftover: lang/compiler/fol-typecheck/src/exprs/calls.rs:3004 emits the diagnostic "imported symbol '<x>' requires workspace-aware typechecking in V1; the legacy single-package path is not sufficient" — a shipped error message referring to 'V1' and a 'legacy path'. Either these are the blessed single-package APIs (rename docs/message) or the path should be deleted.

### 23. Routines chapter keeps 'earlier design' prose under a 'Current V1 routine model' disclaimer
- **file**: `book/src/500_items/200_routines/_index.md`
- **category**: stale-docs
- **evidence**: Line 29 opens "Current `V1` routine model:" and lines 40-42 read "The sections further down that show `result = ...`, short-form omission, last-expression return, and same-name overloading describe earlier design, not the current compiler surface." The stale prose is still live in the same 309-line file: line 64 `... = { result = el1 + el2 - el3 }`, line 67 "Routine can parameter overloaded too", lines 78-89 an overload-resolution walkthrough with `result =` bodies, line 103 another `result =` example. Annotated-not-deleted stale design is the pattern the no-legacy rule forbids.
- **detail**: Lines 28-42: the page is framed as the "Current `V1` routine model" (the project is deep into V3) and explicitly admits "The sections further down that show `result = ...`, short-form omission, last-expression return, and same-name overloading describe earlier design, not the current compiler surface." Keeping not-current design text with a disclaimer is exactly the pattern the no-legacy rule forbids — the stale sections should be deleted, not annotated. (Known to the owner per hardening R12/R13 notes but still present.)

### 24. `fol self default` rewrites the whole config file and accepts unreadable specs
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: write_default_toolchain (161-165) does `fs::write(path, format!("default = {spec}\n"))` — full truncation. read_default_toolchain (147-159) scans lines for a `default` key, and book/src/025_toolchain/_index.md:44 documents the file as key/value. REPRODUCED both halves: (a) with config containing `default = dev` + `extra_key = value`, running `fol self default dev` left the file as just `default = dev` — extra_key silently destroyed; (b) after `fol self link "" <repo>` (which self_link accepts, 593-616), `fol self default ""` printed "default toolchain is now " with exit 0 and wrote `default = $` (cat -A, i.e. empty value), which read_default_toolchain then rejects at line 153 — a default that can never be read back; `fol self which` afterward said "no toolchain selected and no default configured". spec is never validated for emptiness, newlines, or charset.

### 25. Link manifest `std` and `bin` keys are validated then thrown away; a bin-only manifest can't work
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: resolve_linked resolves std_root at 274-282 and validates it at 286-288, then constructs ResolvedToolchain{spec, bin, linked} at 289-293 — std_root is discarded and never reaches the child. dispatch (410-412) sets only DISPATCH_GUARD; the comment at 407-409 confirms "std wiring needs no env". folc resolves std independently: fol-package/src/config.rs:18-30 (exe-sibling std/, else the CARGO_MANIFEST_DIR source path). So `bin = /checkoutB/target/debug/folc` + `std = /checkoutA/lang/library/std` passes every check here and silently compiles against checkout B's std. Half-implemented key set also confirmed: self_link only ever writes `repo = "..."` (612), and a bin-only manifest is rejected at 277-279 with "link '{spec}' has neither `std` nor `repo`" even though the binary's sibling std would be correct.

### 26. $FOL_HOME/pkg is created but nothing uses it; the book documents it as the shared package store
- **file**: `lang/tooling/fol-self/src/main.rs`
- **evidence**: ensure_home_layout (137-143) creates home.join("pkg") on every invocation. Grep for `pkg` across all of lang/tooling/fol-self/src/main.rs returns exactly that one line (138) — no further reference, and no FOL_PACKAGE_STORE_ROOT is ever set on the child (dispatch only sets DISPATCH_GUARD at 412). folc's store defaults elsewhere: fol-frontend/src/fetch.rs:50-59 and build_route/mod.rs:323-327 both fall back to `workspace.root.root.join(".fol").join("pkg")`. book/src/025_toolchain/_index.md:43 claims "pkg/ ← third-party packages, shared by all versions" — false; the directory stays empty.

### 27. Book claims `fol` is implemented by fol-frontend and parses CLI with clap; both false
- **file**: `book/src/050_tooling/100_frontend.md`
- **category**: stale-docs
- **evidence**: Lines 3-5 read "The public FOL entrypoint is the `fol` tool. / `fol` is implemented by the `fol-frontend` crate" — but root Cargo.toml:37-39... specifically [[bin]] at lines 30-32 is `name = "folc"` / `path = "lang/tooling/fol-frontend/src/main.rs"`, and `fol` is built from lang/tooling/fol-self (release.yml:55 `cargo build --release -p fol-self`). Line 19 claims "CLI parsing with `clap`": grep -rn clap --include=Cargo.toml over the whole repo returns ZERO hits; parsing is the hand-rolled ArgCursor in fol-frontend/src/cli/parser.rs:106-111,482. Severity lowered from high to medium — flatly wrong but documentation-only, contradicted by the book's own 025_toolchain chapter.
- **detail**: Lines 3-5: 'The public FOL entrypoint is the fol tool. fol is implemented by the fol-frontend crate' -- stale since the binary split: `fol` is built from lang/tooling/fol-self, fol-frontend builds `folc` (root Cargo.toml [[bin]] name=folc path=lang/tooling/fol-frontend/src/main.rs). Line 19: 'CLI parsing with `clap`' -- fol-frontend/Cargo.toml has no clap dependency; parsing is the hand-rolled ArgCursor parser in src/cli/parser.rs. Contradicts the book's own 025_toolchain chapter.

### 28. bundled-std.md resolution order wrong: .fol/pkg step does not apply to std root
- **file**: `docs/bundled-std.md`
- **category**: docs-truth
- **evidence**: Lines 112-127 present one merged order under "Where Bundled Std Physically Lives" with step 2 = "the project-local fetched store `.fol/pkg`, when it exists" ahead of step 3 = "`std/` next to the running `folc` binary". Std-root resolution never consults .fol/pkg: fol-package/src/config.rs:24-30 is explicit override -> toolchain_std_root (exe-sibling) -> bundled_std_root (compiled-in), and fol-frontend/src/compile/mod.rs:1354-1357 / 1580-1585 is workspace override -> available_bundled_std_root. Only the package-store chain (compile/mod.rs:1346-1353, 1570-1577) inserts `local_store.is_dir()` for .fol/pkg. Runtime claim also confirmed: fol-backend/src/emit/runtime.rs:8-35 keys off FOL_BACKEND_RUNTIME_PATH, not the FOL_STD_ROOT/FOL_PACKAGE_STORE_ROOT named in step 1.
- **detail**: Lines 114-124 give one merged 4-step order and place 'the project-local fetched store .fol/pkg, when it exists' (step 2) ahead of 'std/ next to the running folc binary' (step 3). In code, std root resolution never consults .fol/pkg: fol-package/src/config.rs:24-30 goes explicit override -> exe-sibling std/ -> compiled-in source tree, and fol-frontend/src/compile/mod.rs:1354-1357 goes workspace override -> available_bundled_std_root. Only the package-store-root chain (compile/mod.rs:1346-1353) prefers .fol/pkg. As written, the order is wrong for the thing the section is titled after ('Where Bundled Std Physically Lives'). Runtime crate resolution also uses FOL_BACKEND_RUNTIME_PATH (fol-backend/src/emit/runtime.rs:29-35), not the FOL_STD_ROOT/FOL_PACKAGE_STORE_ROOT envs named in step 1, so 'the same rule applies' is only approximately true.

### 29. Self-described 'legacy single-program surface' pub API with zero non-test callers
- **file**: `lang/compiler/fol-resolver/src/lib.rs`
- **category**: legacy-leftover
- **evidence**: Duplicate of the earlier resolver finding but its extra specifics check out: the 'legacy' labels are at lib.rs:58, 70, 98, 108 on resolve_package / resolve_package_with_config / resolve_prepared_package / resolve_prepared_package_with_config, with mirrored free functions at lib.rs:136-174. grep across the repo for `resolve_prepared_package(` / `resolve_prepared_package_with_config(` outside lang/compiler/fol-resolver/src returns zero hits — genuinely dead outside the crate. resolve_package/_with_config have only fixture/test callers (fol-typecheck/src/model.rs:1424,1458,1497; test/typecheck/test_typecheck.rs; test/resolver/test_resolver.rs; test/integration_tests/integration_pipeline.rs).
- **detail**: fol-resolver/src/lib.rs documents four public entry points as 'This is the legacy single-program surface' (lines 58, 70, 98, 108: Resolver::resolve_package, resolve_package_with_config, resolve_prepared_package, resolve_prepared_package_with_config) plus mirrored free functions at lib.rs:164-173 and session/mod.rs:86-100. `resolve_prepared_package`/`_with_config` have zero callers outside fol-resolver itself; `resolve_package`/`_with_config` are used only by test fixtures (fol-typecheck/src/model.rs:1372+, test/typecheck/test_typecheck.rs). Under the greenfield no-legacy rule, a pub API self-labeled 'legacy' kept alongside the workspace APIs is a compatibility path that should be deleted (tests can build ResolvedProgram via the workspace surface) or, if the single-package fixture path is genuinely wanted, renamed and re-documented without the 'legacy ... prefer the workspace APIs' framing.

### 30. Stale doc comment claims ownership ops coexist with 'legacy #/@ forms until retired'
- **file**: `lang/compiler/fol-typecheck/src/exprs/operators.rs`
- **category**: legacy-leftover
- **evidence**: Duplicate of finding 1, same location operators.rs:574-578, and its supporting sub-claims verified: SYMBOL::Hash exists only as a lexer token (fol-lexer/src/lexer/stage1/element.rs:393 and token/symbol/mod.rs:68) with no consumer anywhere in lang/, so the `#` form is not in the grammar; and `@` is a wanted current shorthand per the project's binding-sigil rule, not a legacy form pending retirement. The comment therefore documents a compatibility posture that does not exist.
- **detail**: The doc comment on `type_ownership_op` (operators.rs:574-578) says the canonical `[opt,...]operand` spelling 'is wired additively alongside the legacy `#`/`@`/implicit forms ... so the two spellings stay semantically identical until the legacy forms are retired.' The `#` form no longer exists anywhere in the grammar (see the dead SYMBOL::Hash finding) and `@` is a wanted current shorthand, not a legacy form pending retirement. The comment documents a migration/compatibility posture the project forbids and misleads maintainers into preserving parity with deleted syntax. Rewrite to describe [opt,...] as the canonical implemented surface.

### 31. `run` buffers child output via .output(): no streaming, no stdin, inconsistent with `test`
- **file**: `lang/tooling/fol-frontend/src/compile/mod.rs`
- **category**: hardening-gap
- **evidence**: compile/mod.rs:891-898 (run_workspace_with_args_and_config) and 983-986 (run_selected_artifact_with_args_and_config) both do `Command::new(&binary).args(args).output()`, then forward_child_output after exit — .output() pipes stdout/stderr and closes stdin, so interactive/TUI/stdin-reading programs see nothing until exit or misbehave. The test route at 1071 does `Command::new(path).status()` with inherited stdio, so the execution paths genuinely diverge. The comment at 895-897 ("`run` should be transparent to the child's stdout/stderr") states an intent the implementation does not meet for streaming or stdin.
- **detail**: run_workspace_with_args_and_config (lang/tooling/fol-frontend/src/compile/mod.rs:892-899) and run_selected_artifact_with_args_and_config (979-986) execute the built binary with .output(), which pipes stdout/stderr (printed only after exit) and closes stdin — long-running or interactive programs (TUI, anything reading stdin) show nothing until exit or misbehave. The test route in the same file uses .status() with inherited stdio (line 1071), so the three execution paths behave differently. This is the documented 'run-doesn't-forward-stdin' gotcha; use .status() (inherit) unless capture is genuinely needed.

### 32. std README.md documents only the 9 bootstrap routines; 16 shipped wrappers and 3 modules missing
- **file**: `lang/library/std/README.md`
- **evidence**: README.md:92-119 "Shipped Surface Summary" lists 3 modules (std.fmt, std.fmt.math, std.io) and 9 routines (fmt::answer/double/triple/sum2, fmt::math::answer, io::echo_int/echo_str/echo_bool/echo_chr). Actual tree: lang/library/std/{fmt,io,strn,term,time,lib.fol} and `grep -rn 'fun\[exp\]'` yields 25 exported routines — lib.fol:4 (env, shell, dir_list, read_file), io/lib.fol:7 (+write, read_key, read_key_ms), term/lib.fol:3 (raw_mode, cols, rows), time/lib.fol:2 (sleep_ms, now_ms), strn/lib.fol:3 (sub, byte_at, from_byte), fmt/root.fol:5 (+int_to_str), fmt/math/lib.fol:1. Line 119 ("Anything outside that list should not be documented as already shipped") and the Growth Rule at 121-129 ("update this README and docs/bundled-std.md") are both violated by the file itself. docs/bundled-std.md:129-139 repeats the same stale 3-module list.

### 33. Empty test body: unified_pipeline_no_glitch_trait_exists asserts nothing
- **file**: `test/integration_tests/integration_diagnostics_pipeline.rs`
- **category**: phantom-green
- **evidence**: Lines 148-159: the whole body of unified_pipeline_no_glitch_trait_exists is comments; there is no statement, no assertion, no scan. Line 155 claims "grep -r \"trait Glitch\" lang/ returns nothing — verified by the build system", but nothing in the function performs that grep, so it is unconditionally green regardless of whether the trait returns. The suggested helper does exist and is used for exactly this kind of scan: collect_text_matches at test/integration_tests/integration_editor_and_build.rs:64-104, called for the stale-needle sweep at :6435.
- **detail**: Lines 148-160: the test body is comments only. It claims 'grep -r "trait Glitch" lang/ returns nothing — verified by the build system', but no scan is performed; the test is unconditionally green forever. Either implement the scan (the collect_text_matches helper used by the stale-alloc test in integration_editor_and_build.rs:64-104 does exactly this) or delete the function. It inflates the pass count while verifying nothing.

### 34. release.yml builds with floating 'stable' Rust and unpinned actions, unlike tests.yml
- **file**: `.github/workflows/release.yml`
- **category**: build-reproducibility
- **evidence**: release.yml:52 `uses: dtolnay/rust-toolchain@stable` versus tests.yml:47-51 which installs and defaults to 1.89.0 (rustup toolchain install 1.89.0 ... rustup default 1.89.0) — released binaries are compiled by a toolchain CI never tested, drifting with each stable release. Action pinning is inconsistent as claimed: release.yml:24, 31, 38, 45 all use `actions/checkout@v4` (mutable tag), while tests.yml:40 and book.yml:20 use `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`. The `gh release create ... 2>/dev/null || true` at release.yml:84-88 does discard all create errors including genuine ones.
- **detail**: release.yml:52 uses dtolnay/rust-toolchain@stable while tests.yml:47-51 pins and tests against 1.89.0 — released binaries are built by a compiler version CI never tested, and it drifts with every stable release. Also release.yml:24/31/38/45 use mutable tag `actions/checkout@v4` whereas tests.yml and book.yml SHA-pin checkout (11bd7190... # v4.2.2) — inconsistent supply-chain hardening in the one workflow that publishes user-facing artifacts. Minor extra: lines 84-88 `gh release create ... 2>/dev/null || true` swallows all create errors (intended for the matrix race, but hides genuine failures until the upload step).

### 35. Book still says the `fol` binary is implemented by fol-frontend (pre-split)
- **file**: `book/src/050_tooling/100_frontend.md`
- **category**: stale-docs
- **evidence**: 100_frontend.md:3-5: 'The public FOL entrypoint is the `fol` tool. `fol` is implemented by the `fol-frontend` crate.' Grep for 'folc' and 'fol-self' in the file returns zero hits, so the page never mentions the split. ARCHITECTURE.md:86-95 documents the correct post-split model (fol from fol-self, folc from fol-frontend/src/main.rs), which this page contradicts.
- **detail**: Lines 3-5: "The public FOL entrypoint is the `fol` tool. `fol` is implemented by the `fol-frontend` crate." After the binary split, `fol` is the toolchain manager built from lang/tooling/fol-self and `folc` is the engine built from fol-frontend (ARCHITECTURE.md:82-92 has this right). The page never mentions folc, fol-self, or the exec-forwarding dispatch, so it describes the pre-split single-binary world.

### 36. Structs chapter contradicts routine rules and links to a nonexistent /docs/spec site
- **file**: `book/src/500_items/300_constructs/200_structs.md`
- **category**: stale-docs
- **evidence**: 200_structs.md:133 states 'the final expression in the function will be used as return value' with link [in function return](/docs/spec/functions/#return); book/src/500_items/200_routines/_index.md:35 says 'last-expression / implicit-tail return is not current' (that disclaimer covers only 'the sections further down' in the routines chapter, not this structs page). 200_structs.md:16 links to /docs/spec/types/#sets. Grep confirms /docs/spec/ URLs also linger at 650_errors/_index.md:43 and 500_items/100_variables.md:63 — none resolve inside this mdbook.
- **detail**: Line 133 states "the final expression in the function will be used as return value" citing [in function return](/docs/spec/functions/#return) — implicit tail return is explicitly not current per book/src/500_items/200_routines/_index.md:33-35. Line 16 links to /docs/spec/types/#sets. Both /docs/spec/... URLs point at an old external doc site layout that does not exist in this mdbook. Old-design prose presented as current behavior.

### 37. CI never runs on the default branch: workflows trigger on 'develop' only, HEAD is 'main'
- **file**: `.github/workflows/tests.yml`
- **category**: stale-config
- **evidence**: tests.yml:4-7 restricts push/pull_request to branches ["develop"]; book.yml:4-6 deploys only on develop. `git ls-remote --symref origin HEAD` returns 'ref: refs/heads/main', and recent commits (b94eba79 etc.) are on main. README.md:9-10 hardcodes blob/develop and badge?branch=develop. Same root defect as the tests-scanner high finding; kept at medium here since that one carries the release-risk framing.
- **detail**: tests.yml lines 4-7 trigger push/pull_request only for branches ["develop"]; book.yml lines 4-6 deploy only on develop. But origin/HEAD points at main and the recent release/merge commits (b94eba79, 5dd4e487, 6088f8c0) landed on main — pushes/merges to main run no tests and never redeploy the book. README.md badges (lines 10-11) also hardcode branch=develop / blob/develop. Either main should be added to the triggers or the default branch story needs to be made consistent.

### 38. No locking in FOL_HOME; concurrent installs share one fixed download path
- **file**: `lang/tooling/fol-self/src/main.rs:531-557`
- **category**: hardening
- **evidence**: main.rs:535: tarball path is the fixed toolchains_root/.download-v{version}.tar.gz. No flock/lockfile logic exists anywhere in the 775-line crate. Two concurrent runs with the same pin both enter install_from_network: both write the same tarball (537), each remove_dir_all's the shared destination (546) during the other's extraction, and line 557 deletes the tarball unconditionally. The success gate at 563 checks only that 'folc' exists, so a half-populated toolchain (missing std/ or runtime/) passes.
- **detail**: The download target is a fixed name, toolchains_root/.download-v{version}.tar.gz (535), and nothing in the crate takes a lock (no lock file anywhere in fol-self). Two concurrent `fol code build` runs in a fresh FOL_HOME with the same pin — trivially common in CI or a multi-pane shell — both enter install_from_network: they write the same tarball concurrently (interleaved bytes), each remove_dir_all's the destination (546) while the other is extracting into it, and whichever finishes first deletes the tarball (557) out from under the other's still-running tar. Outcomes range from "tar failed" to a silently half-populated toolchain that passes the folc-only check at 563. Use a per-process/mkstemp tarball name plus an flock on the destination.

### 39. copy_dir follows symlinks: recurses to ELOOP and leaves a deep partial tree behind (proven)
- **file**: `lang/tooling/fol-self/src/main.rs:702-719`
- **category**: hardening
- **evidence**: Re-proven live: a --from tree with `lang/library/std/selfloop -> ..` made `fol self install 0.5.0 --from <fake>` fail with 'cannot copy .../std/selfloop/std/selfloop/...: Too many levels of symbolic links (os error 40)' and left a 19M partially-populated $FOL_HOME/toolchains/v0.5.0 (folc copied, std/ truncated mid-loop) uncleaned — resolve_installed (296-307) would treat it as installed since folc exists. Root cause: main.rs:711 uses from.is_dir() (follows symlinks) and 714 fs::copy (dereferences); no symlink_metadata, no staged-then-rename.
- **detail**: copy_dir uses `from.is_dir()`, which follows symlinks, and recreates targets as real files/dirs rather than preserving links. PROVEN with a --from tree containing `lang/library/std/selfloop -> ..`: error: cannot copy .../std/selfloop/std/selfloop/... : Too many levels of symbolic links (os error 40) and $FOL_HOME/toolchains/v0.5.0/std/selfloop/std/selfloop/... left on disk, uncleaned, after the failing install. The same behaviour means a symlink in the source tree pointing outside the repo gets dereferenced and its contents copied into the toolchain. Use symlink_metadata, and either preserve or reject symlinks; combine with the staged-then-rename fix so failures leave nothing behind.

### 40. FOL_DISPATCHED is never cleared, so it poisons every descendant process (proven)
- **file**: `lang/tooling/fol-self/src/main.rs:373-379`
- **category**: hardening
- **evidence**: Re-proven live: `FOL_DISPATCHED=1 fol code build` -> 'error: recursive fol dispatch detected — a toolchain binary invoked `fol` again.' exit 1. dispatch() sets the guard at main.rs:412 and execs folc (417); workspace-wide grep for FOL_DISPATCHED hits only fol-self/src/main.rs:13, so folc never unsets it — cargo, build steps, and programs launched by `fol code run` all inherit it, and any legitimate nested `fol` invocation in that tree is rejected with a misdiagnosing message.
- **detail**: dispatch sets FOL_DISPATCHED=1 and exec's folc (412-418); nothing ever unsets it (grep: the variable is referenced nowhere outside this file, so folc does not clear it either). Every process folc spawns — cargo, a build step, and any FOL program launched by `fol code run` — inherits it, and any legitimate nested `fol` invocation in that tree is rejected. PROVEN: $ FOL_DISPATCHED=1 fol code build error: recursive fol dispatch detected — a toolchain binary invoked `fol` again. (exit 1) That message is also a misdiagnosis in this case. A depth counter (FOL_DISPATCH_DEPTH, fail above N) or having folc remove the variable before spawning children would keep the real protection without banning nested use.

### 41. ARCHITECTURE.md still calls fol-frontend 'the CLI binary (fol)', contradicting its own split description
- **file**: `ARCHITECTURE.md`
- **category**: stale-docs
- **evidence**: ARCHITECTURE.md:482-484: 'It is the CLI binary (`fol`) and the orchestration point ... based on the user's command (`build`, `run`, `test`, `check`, `edit`)' — contradicts the correct split text at lines 86-95 in the same file. The stale command list also appears in the diagram at line 268 ('command dispatch (build, run, test, check, edit)'); the real command groups are work/pack/code/tool and no `edit` command exists in cli/parser.rs.
- **detail**: Line 481-484 ('fol-frontend orchestrates everything ... It is the CLI binary (`fol`) and the orchestration point ... based on the user's command (build, run, test, check, edit)') is pre-split text. Lines 87-95 of the same file correctly say fol comes from fol-self and folc from fol-frontend/src/main.rs. Also the listed commands (build, run, test, check, edit) are stale -- the real groups are work/pack/code/tool and there is no `edit` command.

### 42. Shipped `fol tool complete` command missing from book lists that claim to be exhaustive
- **file**: `book/src/050_tooling/200_tool_commands.md`
- **category**: docs-truth
- **evidence**: The command exists: fol-frontend/src/cli/parser.rs:390 help entry (c7 = cmd("complete"), rendered as 'Get completions at a position') and parser.rs:1268 dispatch to ToolSubcommand::Complete. Both book lists omit it: 200_tool_commands.md:154-166 (lsp/format/parse/highlight/symbols/references/rename/semantic-tokens/tree generate/clean/completion — no `complete`) and 300_editor.md:14-26 (same). 200_tool_commands.md:230 asserts 'Only the shipped `fol tool` subcommands above are public.' — the exhaustiveness claim is false. (Detail's parser.rs:372 is off by a few lines; entry is at 390.)
- **detail**: The tool-group lists at 200_tool_commands.md:154-166 and 300_editor.md:14-26 omit `fol tool complete` ('Get completions at a position'), yet 200_tool_commands.md:227-230 asserts 'Only the shipped `fol tool` subcommands above are public.' The command is real and shown in `fol tool --help`: fol-frontend/src/cli/parser.rs:372 (help entry, cmd("complete")) and parser.rs:1268 (ToolSubcommand::Complete dispatch). Either the claim of exhaustiveness or the list is wrong.

### 43. Dead 'legacy_' accessors on Diagnostic used only by their own unit tests
- **file**: `lang/compiler/fol-diagnostics/src/model.rs`
- **category**: dead-code-legacy
- **evidence**: model.rs:222-229: legacy_location() and legacy_help() are pure aliases of primary_location()/first_help(). Workspace-wide grep finds callers only inside fol-diagnostics itself: lib.rs:345 (test named test_coded_report_helpers_preserve_legacy_location_and_help_views), lib.rs:365, 373, 477. Nothing outside the crate consumes the 'legacy views' — direct violation of the greenfield no-legacy rule.
- **detail**: `Diagnostic::legacy_location()` (model.rs:222) and `Diagnostic::legacy_help()` (model.rs:226) are pub methods whose only callers in the entire repo are fol-diagnostics' own tests (lib.rs:345-477, a test literally named `test_coded_report_helpers_preserve_legacy_location_and_help_views`). They are pure aliases of primary_location()/first_help() kept as 'legacy views'. Greenfield rule: delete the methods and the preservation test; nothing consumes the legacy view.

### 44. Package-store/std/runtime path resolution duplicated across 6+ sites with confirmed drift
- **file**: `lang/tooling/fol-frontend/src/compile/mod.rs`
- **category**: duplication-drift
- **evidence**: All six sites verified: (1) compile/mod.rs:1346-1353 full chain with local-store is_dir gate + available_bundled_store_root fallback, spelled .join(".fol").join("pkg"); (2) compile/mod.rs:1570-1577 same chain re-implemented, spelled .join(".fol/pkg"); (3) build_route/mod.rs:323-327 has NO is_dir gate and NO bundled fallback — real behavioral drift vs the compile route; (4) fetch.rs:50-59 select_package_store_root with no bundled fallback; (5) fol-editor/src/lsp/analysis.rs:228-238 uses only package_root.join(".fol/pkg") when is_dir, no config overrides, no bundled fallback; (6) current_exe-sibling probe duplicated between fol-package/src/config.rs:19-22 (std/, is_dir) and fol-backend/src/emit/runtime.rs:16-21 (runtime/, src/lib.rs is_file) with different existence checks.
- **detail**: The store-root fallback chain (config override -> workspace override -> local .fol/pkg -> bundled store) is re-implemented independently: (1) lang/tooling/fol-frontend/src/compile/mod.rs:1345-1357 (validate_build_dependency_queries) and (2) 1568-1587 (resolver_config) — two copies in the same file (one spells .join(".fol").join("pkg"), the other .join(".fol/pkg")); (3) lang/tooling/fol-frontend/src/build_route/mod.rs:323-331 — MISSING both the local-store is_dir gate and the available_bundled_store_root fallback, so build-route dependency-query validation rejects deps the compile route accepts; (4) lang/tooling/fol-frontend/src/fetch.rs:50-59 (select_package_store_root, no bundled fallback); (5) lang/tooling/fol-editor/src/lsp/analysis.rs:228-238 — LSP uses only package_root.join(".fol/pkg") when it is a dir: no config overrides and no bundled-store fallback, so store packages that build fine via folc show unresolved in the editor; (6) the toolchain current_exe-sibling probe is duplicated between lang/compiler/fol-package/src/config.rs:17-30 (std/, checks is_dir) and lang/execution/fol-backend/src/emit/runtime.rs:8-27 (runtime/, checks src/lib.rs is_file) with different existence checks. Extract one shared resolver.

### 45. CLI silently drops trailing non-flag arguments after a direct input token
- **file**: `lang/tooling/fol-frontend/src/cli/parser.rs`
- **category**: bug
- **evidence**: Reproduced live: `folc run main.fol` prints 'Compiling: run' then F1004 'File not found: run' and never mentions main.fol. Code confirms: parser.rs:537-549 — None fallback treats the first unknown token as cli.input, then the loop consumes only tokens starting with "--" and silently `break`s on the first non-flag token; no 'unknown command' or 'unexpected argument' error path exists.
- **detail**: In the root-command fallback (lang/tooling/fol-frontend/src/cli/parser.rs:536-549), an unrecognized first token is treated as direct file input, and the follow-up loop consumes only `--flags`, breaking on the first non-flag token without any error. Verified: `folc run main.fol` compiles a file literally named 'run' (F1004 'File not found: run') and never mentions the ignored 'main.fol'. Unconsumed tokens should be a hard error ('unknown command run' or 'unexpected argument main.fol').

### 46. byte_to_str violates its own contract for bytes 128-255
- **file**: `lang/execution/fol-runtime/src/std.rs`
- **evidence**: std.rs:433-444: doc comment says 'A one-byte string from a byte value (empty outside 0-255)'; for byte 128-255 the else branch returns String::from_utf8_lossy(&[byte]) which is always "\u{FFFD}" (a 3-byte replacement char) since any lone byte >=128 is invalid UTF-8 — the branch can never produce the requested byte, and str_byte (std.rs:425-431) -> byte_to_str round-trips silently fail for all non-ASCII bytes. Catalog repeats the false contract at fol-intrinsics/src/catalog.rs:775, and std wrapper strn::from_byte (lang/library/std/strn/lib.fol:9-10) inherits it.

### 47. Book builtin page documents intrinsics 57-64 but not 65-72
- **file**: `book/src/300_meta/100_buildin.md`
- **evidence**: 100_buildin.md:172-193 ('Terminal and OS hooks') documents write/read_key/raw_mode/sleep_ms/now_ms/term_cols/term_rows/int_to_str and closes with an exhaustive-reading wrapper list ('The bundled std package wraps them as ...', lines 190-193). Grep of the file finds zero mentions of str_sub, str_byte, byte_to_str, read_key_ms, env_var, shell, dir_list, read_file, or std.strn — yet all eight exist in fol-intrinsics/src/catalog.rs (lines 743, 755, 767, 779, 791, 803, 815, 827) with shipped std wrappers (lang/library/std/lib.fol:1-14, strn/ module).

### 48. No test or example exercises read_key, read_key_ms, raw_mode, term::cols, term::rows
- **file**: `test/integration_tests/integration_v3_runtime_proofs.rs`
- **evidence**: Repo-wide grep across test/ and examples/ (all file types, including .fol embedded in .rs) returns zero hits for read_key, read_key_ms, raw_mode, term::cols, term::rows. integration_v3_runtime_proofs.rs covers the other hooks (write/int_to_str/sleep_ms/now_ms, strn, env/shell, dir_list/read_file) but not these five public std names. lang/library/std/README.md Growth Rule (lines ~122-130) requires 'a real example package' and 'CLI/integration coverage' for every new public name — violated.

### 49. Ignore reason on first GitHub-fetch test is factually wrong, and the test is redundant
- **file**: `test/integration_tests/integration_editor_and_build.rs`
- **category**: stale-annotation
- **evidence**: Line 7639: #[ignore = "exercises git fetch through a local fixture remote"] on test_frontend_fetches_public_logtiny_from_github, whose body (7644-7648) fetches the real https://github.com/bresilla/logtiny.git (tag:v0.1.4 + hash 7b8fe6b033e4) — the reason string matches the genuinely-hermetic fixture test at :5237-5245 instead; running `--ignored` tests believing them local hits the network. The sibling at :7671 has the correct reason. Redundancy is slightly overstated (this test also asserts the 'prepared 1 workspace package' summary and tag+hash combined, while the matrix covers tag and hash separately), but the mislabeled annotation is the confirmed defect.
- **detail**: Line 7639: #[ignore = "exercises git fetch through a local fixture remote"] — but the body (:7641-7648) fetches the real https://github.com/bresilla/logtiny.git with tag:v0.1.4 + hash. The reason string describes the hermetic tests at :5237, not this one. Additionally, its tag+hash coverage is fully subsumed by the matrix test at :7671 (cases 'tag' and 'hash'). Fix the reason on :7671's sibling or delete :7638-7666 outright.

### 50. Sibling checkouts parc/linc/gerc are hard workspace prerequisites — fresh clone cannot run any test
- **file**: `lang/tooling/fol-interop/Cargo.toml`
- **category**: test-environment
- **evidence**: fol-interop/Cargo.toml:15-17: non-optional path deps ../../../../{gerc,linc,parc} (no feature gate), and fol-interop is a root workspace member (Cargo.toml:20), so a missing sibling breaks cargo metadata and with it every root cargo command. SHAs are hand-pinned in tests.yml:30/37/44 and release.yml:34/41/48, currently matching interop.lock.toml:8/18/29 (parc 0f52aeee, linc c874d5b0, gerc 000c1f6c); only tests.yml's `make verify` -> tools/verify-interop-lock.sh (Makefile:89-94) would catch drift at runtime — nothing keeps the copies in sync.
- **detail**: Lines 15-17 declare path deps '../../../../{gerc,linc,parc}' (i.e. siblings of the fol checkout). Because fol-interop is a workspace member, a missing sibling breaks 'cargo metadata', so EVERY root cargo command (build/test/clippy, even for unrelated crates) fails — there is no feature gate or graceful degradation. tests.yml (lines 26-44) and release.yml (lines 29-50) each hand-pin the three SHAs; they currently match interop.lock.toml (revisions at lines 8/18/29) but nothing automated keeps the workflow pins and the lock in sync — only the runtime failure of tools/verify-interop-lock.sh --locked would catch drift.

### 51. cliff.toml links changelog to the wrong repository (bresilla/roc)
- **file**: `cliff.toml`
- **category**: legacy-leftover
- **evidence**: cliff.toml:26: { pattern = '<REPO>', replace = "https://github.com/bresilla/roc" } — this repo's remote is git@github.com:fol-lang/fol.git. The commit_preprocessor at line 39 rewrites '(#123)' into '<REPO>/issues/123' links, so generated changelog issue links point at a foreign repo. Confirmed as a template leftover; severity adjusted high->medium since the blast radius is changelog generation only, not shipped code or release artifacts.
- **detail**: cliff.toml:26 postprocessor: `{ pattern = '<REPO>', replace = "https://github.com/bresilla/roc" }` — leftover from a template for a different project ('roc', different org too; this repo's remote is fol-lang/fol). The commit_preprocessor at line 39 rewrites '(#123)' into issue links via <REPO>, so any issue reference in CHANGELOG.md points at a foreign repository.

### 52. parc/linc/gerc SHAs duplicated in three places; release.yml never verifies them against the lock
- **file**: `.github/workflows/release.yml`
- **category**: single-source-of-truth
- **evidence**: Three independent copies verified identical today: interop.lock.toml:8/18/29, tests.yml:30/37/44, release.yml:34/41/48 (parc 0f52aeeeeec4..., linc c874d5b03322..., gerc 000c1f6c12fb...). tests.yml runs `make verify` which invokes tools/verify-interop-lock.sh (Makefile:89-94); grep of release.yml finds no verify/lock step at all — it checks out the hand-pinned refs and builds, so a SHA edited only in release.yml would ship a toolchain built against revisions the lock never certified.
- **detail**: The sibling revisions live in interop.lock.toml (lines 8, 18, 29), tests.yml (lines 30, 37, 44) and release.yml (lines 34, 41, 48). All three copies currently agree (parc 0f52aeee, linc c874d5b0, gerc 000c1f6c), but nothing derives one from another. tests.yml is self-checking because `make verify` runs tools/verify-interop-lock.sh (Makefile:89-94), which would catch drift; release.yml runs no lock verification at all, so a stale SHA edited only in release.yml would silently ship a toolchain built against different parc/linc/gerc than the lock certifies. Workflows should read the SHAs from interop.lock.toml (or at least release.yml should run interop-locked before packing).

### 53. .gitignore contradicts the index: docs/ and plan are ignored yet tracked; book/book not ignored
- **file**: `.gitignore`
- **category**: repo-hygiene
- **evidence**: (1) .gitignore:5 'docs/' while 8 files under docs/ are tracked (git ls-files: bundled-std.md, editor-sync.md, runtime-models.md, five v2-*.md); git check-ignore --no-index -v confirms the rule matches them, so new files under docs/ are invisible to git status; book.yml:28,34 additionally uses docs/ as the mdbook --dest-dir/publish_dir scratch, and the v2-*.md files are stale V2-era notes. (2) .gitignore:11 'plan' while plan/V4_PLAN.md is tracked (same check-ignore match) — a live plan hidden behind an ignore rule. (3) `git check-ignore book/book` exits 1: the default local mdbook output is not ignored.
- **detail**: (1) .gitignore:5 `docs/` — but 8 files under docs/ are tracked (bundled-std.md, editor-sync.md, runtime-models.md, v2-*.md); `git check-ignore --no-index` confirms the rule matches them. The rule is dead for the tracked files and actively hides any NEW file added under docs/ from git status; worse, book.yml uses docs/ as its CI build-output scratch dir. The v2-*.md files themselves are stale V2-era design notes in a project whose greenfield rule forbids keeping replaced-material references. (2) .gitignore:11 `plan` — but plan/V4_PLAN.md is tracked; same dead-rule/hidden-new-files contradiction (a live V4 plan hidden behind an ignore rule). (3) book/book — the default output of a plain local `mdbook build book` — is NOT ignored (`git check-ignore book/book` exits 1), a known artifact-noise gotcha. Decide per-path: either untrack or unignore.


## CONFIRMED — LOW (7)

- **ARCHITECTURE.md workspace layout omits the fol-interop crate** — `ARCHITECTURE.md` — The 'tooling/' block (ARCHITECTURE.md:81-85) lists only fol-self, fol-frontend, fol-editor; grep for 'interop' in ARCHITECTURE.md matches only line 505 (a V4 roadmap bullet). fol-interop is a workspace member (root Cargo.toml:20) and a direct dependency of fol-frontend (lang/tooling/fol-frontend/Cargo.toml:26). The Tooling-layer diagram's fol-frontend dep list (ARCHITECTURE.md:270-271: 'package, stream, lexer, parser, resolver, typecheck, lower, backend, editor, diagnostics') omits fol-build (Cargo.toml:22), fol-types (:25), and fol-interop (:26). Doc-completeness defect; low severity.
- **Bootstrap placeholder functions still shipped in std.fmt** — `lang/library/std/fmt/root.fol` — fmt/root.fol ships answer() (delegates to math::answer()), double(), triple(), sum2() (lines 3-17), and fmt/math/lib.fol:1-3 returns the literal 7 — four of the five fmt exports are proof-of-plumbing scaffolding next to the one real routine int_to_str (:19-21). Confirmed as sloppy shipped surface, but downgraded medium→low: these are documented bootstrap scaffolding (docs/bundled-std.md and std README treat them as the deliberate bootstrap surface, with example packages consuming them), not a replaced/legacy path — removal is a cleanup decision, not a compat leak.
- **Release/toolchain contract is guarded only by string-matching the workflow YAML** — `test/integration_tests/integration_cli_compile.rs` — test_release_workflow_ships_fetchable_toolchain_artifacts (lines 81-114) reads .github/workflows/release.yml as a string and asserts substrings: 'cargo build --release --bin folc', 'cargo build --release -p fol-self', '-C "$STAGE" folc std runtime', 'x86_64-linux'/'aarch64-linux', and absence of 'macos'. test_workspace_crates_keep_authors_metadata follows at :116. Pure text coupling as described — a broken staging script passes while a behavior-preserving YAML rewording fails. Confirmed, but severity lowered to low: it is a deliberate cheap contract lock; the substantive gap (no test stages the tarball and runs the install path) is the separate fol-self coverage finding.
- **Plain 'cargo test' at root silently skips 16 member crates' test suites** — `Cargo.toml` — Cargo.toml:3 `default-members = [".", "lang/tooling/fol-self"]` (17 members listed at :4-21), so bare `cargo test` runs only the root integration suite plus fol-self's 7 unit tests. Makefile:109-110: `cargo test --workspace $(TEST_ARGS)` then `cargo test -- $(TEST_ARGS) --ignored` — the ignored pass indeed lacks --workspace. Confirmed, but low: CI runs `make verify` (--workspace), default-members exists to make `cargo build` produce fol+folc (commit b94eba79), and the finding itself concedes the ignored-pass asymmetry is latent (only the root suite has #[ignore] tests today).
- **roadmap_for_intrinsic classifies all implemented hooks (57-72) as future V2** — `lang/compiler/fol-intrinsics/src/catalog.rs` — catalog.rs:885-894 ends with `_ => IntrinsicRoadmap::V2`, and no arm covers 57..=72 (max IntrinsicId in the registry is 72). Those entries are live: e.g. IntrinsicId::new(57) "write" at 645-656 and new(58) "read_key" at 657-668 are IntrinsicAvailability::V1 + IntrinsicStatus::Implemented + IntrinsicLoweringMode::RuntimeHook, as are raw_mode(59), sleep_ms(60), now_ms(61), term_cols(62), term_rows(63), int_to_str(64), str_sub(65), str_byte(66), read_key_ms, env_var, shell, dir_list, read_file. So shipped hooks share the V2 bucket with the genuinely unimplemented bitwise family (37..=56) — a real metadata inconsistency. Severity lowered to low: grep shows the only consumers of roadmap_for_intrinsic/intrinsics_for_roadmap are fol-intrinsics' own tests (lib.rs:445-491); no shipped surface (including `fol explain`) reads it yet, so the impact is latent.
- **Missing obvious CLI/TUI primitives: argv, stderr, exit, float formatting, str parse/find, file write** — `lang/compiler/fol-intrinsics/src/catalog.rs` — All six absences verified. grep of catalog.rs for "args"/"argv"/"stderr"/"exit"/float_to_str/str_to_int/str_to_float/write_file/str_find/str_split returns nothing, and the complete runtime surface (`grep '^pub fn' lang/execution/fol-runtime/src/std.rs`) is echo, write, read_key, read_key_ms, str_sub, str_byte, byte_to_str, raw_mode, sleep_ms, now_ms, term_cols, term_rows, int_to_str, env_var, shell, dir_list, read_file (+ task/tier helpers) — no argv, no stderr, no exit, no float formatting or numeric parsing, no fs write, no find/split. The argv gap is a genuine inconsistency, since `fol code run` forwards args to the child (compile/mod.rs:892) that the program has no way to read. Severity lowered from medium to low: this is an enhancement/roadmap list, not a defect — nothing behaves incorrectly today.
- **Dead lexer token: '#' still tokenized as SYMBOL::Hash but consumed by nothing** — `lang/compiler/fol-lexer/src/lexer/stage1/element.rs` — element.rs:393 maps '#' -> Symbol(SYMBOL::Hash); variant declared at lang/compiler/fol-lexer/src/token/symbol/mod.rs:32 and rendered at mod.rs:68. Workspace grep for SYMBOL::Hash returns exactly those 3 sites; fol-parser's full SYMBOL:: usage list (grep over lang/compiler/fol-parser/src) contains no Hash. Real, but downgraded to low: SYMBOL::Degree, SYMBOL::Sign, and SYMBOL::And are in the identical 2-hit unused state, so this is one instance of a broader dead-token pattern, and '#' in source already yields a normal parse error rather than misbehavior.

## UNVERIFIED LOW (39)

Reported by scanners, not put through adversarial verification.

- **plan gitignored but plan/V4_PLAN.md is tracked while V3_MEM/V3_PROC/TOOLCHAIN are not** — `.gitignore` — .gitignore line 11 ignores 'plan', yet plan/V4_PLAN.md is tracked (git ls-files) while plan/V3_MEM.md, plan/V3_PROC.md and plan/TOOLCHAIN.md show as ignored-untracked (git status --ignored: '!!'). One plan file is public, three are machine-local — inconsistent; either track the plan directory deliberately or untrack V4_PLAN.md.
- **xtra/ only ignored via the user's personal global gitignore; internal version mismatch in logtiny** — `xtra/logtiny/PROJECT` — xtra/ is not in the repo .gitignore; it is ignored only by ~/.config/git/ignore:33 (git check-ignore -v). On any other clone/CI the vendored xtra/logtiny package appears as untracked noise. Inside it, PROJECT declares version 0.1.0 while build.fol meta says version = "0.1.4" — the local copy has drifted from the published github.com/bresilla/logtiny it mirrors (referenced by lang/library/std/README.md:46). Either add xtra/ to the repo .gitignore or delete the stale copy.
- **mdbook output dir book/book is not gitignored** — `.gitignore` — book/book (mdbook's default build-dir for book/book.toml, which sets no custom build.build-dir) is not matched by any ignore rule (git check-ignore book/book exits 1) and there is no book/.gitignore. A local `mdbook build` produces a large untracked artifact tree that can be committed by accident — this exact gotcha is already recorded in the project's own working notes. Add book/book/ to .gitignore.
- **FOL_STD_ROOT env override survives in folc but is unused, untested by CI, and undocumented in the book** — `lang/tooling/fol-frontend/src/config.rs` — config.rs:87 still wires the FOL_STD_ROOT env var into std_root_override (plus the guard list at lang/tooling/fol-frontend/src/test_env.rs:7 and a unit test at config.rs:160). Nothing in test/, .github/, Makefile, or fol-self sets it; the toolchain design (book/src/025_toolchain/200_versions.md, fol-self main.rs:407 'std wiring needs no env: folc resolves std/ next to its own binary') deliberately avoids env-based std wiring, and the only place FOL_STD_ROOT is documented is the gitignored docs/bundled-std.md:117. The --std-root CLI flag covers the dev/test use case. Verify intent: if the env knob predates the toolchain split, remove it; if kept, document it in the book's toolchain chapter.
- **Dead non-unix dispatch branch in a Linux-only-forever project** — `lang/tooling/fol-self/src/main.rs:414-426` — dispatch carries a `#[cfg(not(unix))]` Command::status() fallback (420-426) alongside the exec path. FOL is Linux-only by decision (book/src/025_toolchain/_index.md:88 "FOL is Linux-only"; the release matrix builds only x86_64-linux/aarch64-linux), so this branch can never compile in a supported configuration — a portability compatibility path of exactly the kind the greenfield rule forbids. Drop it and the #[cfg(unix)] wrapper with it.
- **Pin parser: BOM silently disables the pin, prose comments can yield a bogus version, doc says "first comment line"** — `lang/tooling/fol-self/src/main.rs:184-203` — Three concrete edges: (1) a build.fol saved with a UTF-8 BOM starts the first line with U+FEFF, which `trim()` does not strip (it is Cf, not whitespace), so strip_prefix("//") fails and the function returns None at line 191 — the pin is silently ignored and the project builds with the default toolchain instead of erroring. (2) Any leading-block comment whose text begins with "fol " is parsed as a pin: `// fol build manifest` yields spec "build", producing "toolchain 'build' is not installed". The existing test only covers `// the fol build manifest` (line 746), where "fol" isn't first. (3) The loop scans the entire leading comment/blank block, while book/src/025_toolchain/_index.md:52 says the pin lives on "the first comment line of build.fol" — code and doc disagree. CRLF itself is handled correctly (trim eats the \r).
- **'v' prefix handling differs between the source and network installers** — `lang/tooling/fol-self/src/main.rs:472` — self_install strips exactly one leading 'v' from the argument, then install_from_source normalizes again via normalize_version_dir (502) while install_from_network re-adds a 'v' by hand in the URL (533). For `fol self install vv0.2.0`: looks_like_version passes (strip one 'v' -> "0…"), the source path installs into toolchains/v0.2.0, but the network path requests .../download/vv0.2.0/fol-compiler-and-lib-vv0.2.0-x86_64-linux.tar.gz and fails with a 404-shaped error suggesting `fol self install v0.2.0 --from <repo>`. One normalization helper should own the 'v' rule for the dir name, the URL tag, and the asset name.
- **The manager has no way to report its own version** — `lang/tooling/fol-self/src/main.rs:388-395` — `-V`/`--version` are help_like, so when a toolchain resolves they are forwarded to folc; `fol --version` prints "fol 0.2.2" — the ENGINE's version, labelled "fol". There is no `fol self version`, no `fol self update`, and no distinguishable output, so a user (or a bug report) cannot tell which manager binary is installed even though the manager and engine are versioned and shipped as separate release assets (fol-v* vs fol-compiler-and-lib-v*). print_fallback_help (69-70) advertises -V/--version under the manager's own Options, which reinforces the wrong impression.
- **self subcommands silently ignore stray/duplicate arguments** — `lang/tooling/fol-self/src/main.rs:461-485` — self_install accepts a repeated --from with last-wins and no complaint (466-470); self_which (680) and self_list (628) take no args at all yet accept and discard them — PROVEN: `fol self which bogus` prints the resolved folc and exits 0, so a user who meant "which folc does toolchain X use" gets an answer about a different toolchain. Every other subcommand uses exact slice patterns ([spec], [name, repo]) and errors properly, so this is an inconsistency inside the same command family. Reject unexpected arguments.
- **installed_toolchains treats any *.toml as a link and sorts versions lexicographically** — `lang/tooling/fol-self/src/main.rs:327-343` — Any file ending in .toml under toolchains/ is reported as a link — an editor backup, a stray Cargo.toml, or the `.toml` file an empty link name creates all show up in `fol self list` and are resolvable specs. Directories lacking `folc` are invisible to list/selection even though self_remove can still delete them (so a half-extracted toolchain cannot be seen, only guessed). The plain `sort()` on strings also orders v0.10.0 before v0.9.0, which matters beyond display because select_toolchain_spec's "exactly one installed" shortcut (360-362) reads from this same vector. That shortcut is itself undocumented in the selection order printed at line 96 (and in README), while book/src/025_toolchain/_index.md:80 does list it — so the tool's own help omits rule 5.
- **FOL_HOME is accepted without canonicalization or sanity checks** — `lang/tooling/fol-self/src/main.rs:107-131` — fol_home() only rejects a byte-empty value; a whitespace-only FOL_HOME=" " becomes the directory " " and a relative FOL_HOME="fol-home" makes toolchain resolution depend on the process CWD, so `fol self install` and a later `fol code build` from a subdirectory silently use different homes (install_from_source canonicalizes the --from repo but never the home). Paths with spaces are fine because everything goes through PathBuf/OsStr. Canonicalize (or at least require absolute) once in fol_home() and report a clear error otherwise.
- **No tests cover any destructive or dispatching path in the manager** — `lang/tooling/fol-self/src/main.rs:721-775` — The only tests are 8 unit tests over pure helpers (pin parsing, version normalization, link-manifest parsing). Nothing exercises install/install --from/remove/link/default/dispatch, and there is no integration test for the manager anywhere — grep for CARGO_BIN_EXE_fol or fol-self across test/ finds nothing; the sole FOL_HOME mention in test/integration_tests/integration_editor_and_build.rs:5550 only asserts the book mentions the string. Given that these untested paths call fs::remove_dir_all on a caller-supplied path and rewrite $FOL_HOME/config, the traversal, atomicity, and config-parse bugs above were all reachable with a single command each. A tmp-FOL_HOME harness driving the real binary would have caught every one of them.
- **Install instructions pin v0.2.1 while v0.2.2 is the current tagged release** — `README.md` — README.md:31-33 and book/src/025_toolchain/100_install.md:10-11 tell users to curl fol-v0.2.1-x86_64-linux, but git tag v0.2.2 exists and workspace version is 0.2.2 (Cargo.toml:2). All surrounding examples (layout, pin, fol self install) also say 0.2.1. Not wrong as an example, but the front-door install command fetches a superseded manager binary.
- **CHANGELOG gaps: no [0.1.3] section despite v0.1.3 tag; stray unprefixed '0.2.0' tag outside the release scheme** — `CHANGELOG.md` — Sections present: 0.2.2, 0.2.1, 0.1.4, 0.1.2, 0.1.1 -- v0.1.3 is a real tag but has no section. A bare '0.2.0' git tag (no v prefix) exists; the release workflow only triggers on 'v*' so it was never released, and `fol self install 0.2.0` would 404 against fol-compiler-and-lib-v0.2.0-* assets. Additionally the [0.2.1] section is a single ~3260-line dump covering all work from 2024-03 to 2026-07 (V1+V2+V3), which makes the changelog nearly useless for locating when anything landed; and the post-v0.2.2 fix b94eba79 (default-members so `cargo build` produces both fol and folc) is not reflected anywhere.
- **README selection order omits the single-installed-toolchain fallback** — `README.md` — README.md:56-58 states selection order ends at 'the configured default', but fol-self/src/main.rs:360-363 adds a final step: if exactly one toolchain is installed it is used with no default configured. The book documents this correctly as step 5 (book/src/025_toolchain/_index.md:77) and the 200_versions.md:30 dispatch diagram omits it too.
- **055_build chapter lists api.rs/eval.rs/executor.rs which are now directories; native.rs missing** — `book/src/055_build/_index.md` — 'What fol-build Owns' (lines 68-79) names graph.rs, api.rs, semantic.rs, stdlib.rs, executor.rs, eval.rs, option.rs, runtime.rs, step.rs, codegen.rs, artifact.rs, dependency.rs. Actual lang/execution/fol-build/src has api/, eval/, executor/ as directories (not .rs files) and also contains native.rs, which the list omits even though the chapter elsewhere claims fol-build owns 'native artifact declarations'.
- **Stale clap-behavior comments in the hand-rolled CLI parser** — `lang/tooling/fol-frontend/src/cli/parser.rs` — parser.rs:1021-1022 comments explain behavior in terms of clap ('but clap's behavior with trailing_var_arg...', 'Actually with clap, `fol code run -- --flag value` puts --flag into target.input') but the crate has no clap dependency -- the parser is fully hand-rolled. Leftover from the removed clap implementation; in a greenfield no-legacy repo these references to a replaced mechanism are defects.
- **CI never tests main or release tags: tests.yml/book.yml trigger only on develop** — `.github/workflows/tests.yml` — tests.yml triggers on push/PR to 'develop' only; book.yml deploys only from develop; release.yml triggers on v* tags and builds/uploads without running any test gate. The repo's default/PR branch is main (recent release commits including v0.2.2 land there), so pushes to main and tagged releases are completely untested by CI, and the README tests badge (branch=develop) does not reflect the branch releases are cut from.
- **User-facing diagnostics leak internal roadmap jargon: 'V2 Milestone 1/2', 'not lowered in this slice yet'** — `lang/compiler/fol-typecheck/src/exprs/calls.rs` — While plain V1/V2/V3 labels appear in the public book and are arguably sanctioned, several errors leak deeper internal planning vocabulary that means nothing to a user: fol-typecheck/src/exprs/calls.rs:2529 and :2939 say 'in V2 Milestone 1', fol-typecheck/src/decls.rs:1487 says 'in V2 Milestone 2', fol-lower/src/exprs/calls.rs:972 says "are not lowered in this slice yet" and fol-lower/src/exprs/flow.rs:1465 says "type-matching when/of branches are not lowered in this slice yet" ('slice' is the team's internal work-planning term; 'lowered' is compiler-internal phase jargon). Additionally the version tags are internally inconsistent/stale: 72 messages across fol-typecheck/fol-lower still say 'in V1' or 'in V2' (e.g. literals.rs:46 'nil literals require an expected opt[...] or err[...] shell type in V1') for behavior that is the current V3-era contract — mixed V1/V2/V3 labels on live rules read as three different products.
- **Unknown string escape sequences are silently kept as literal text instead of being diagnosed** — `lang/compiler/fol-parser/src/ast/parser_parts/expression_atoms_and_literal_lowering.rs` — PROBE-CONFIRMED: `var s: str = "bad\q escape"` compiles cleanly and the built binary prints `bad\q escape` — the decoder's fallback branches (expression_atoms_and_literal_lowering.rs ~585-650: every failed decode does `decoded.push('\\'); decoded.push(...)`) silently preserve the backslash for any unrecognized or malformed escape (\q, truncated \x, bad \u{...}), while \n/\t/\" are interpreted. A typo'd escape thus changes program output with no warning. For a language that rejects everything else loudly, unknown escapes should be a parse error (or at minimum a diagnostic), matching the strictness of the rest of the surface.
- **Spawn-capture and dfr/edf-capture validation are near-duplicate blocks that must be kept in sync by hand** — `lang/compiler/fol-typecheck/src/exprs/mod.rs` — The per-operation capture checks are written twice in fol-typecheck/src/exprs/mod.rs: the spawn/closure path (~lines 1189-1260: mov marks moved, cpy requires type_lacks_copy with message at :1206, cln requires ownership_moves_on_transfer + type_lacks_clone) and the dfr/edf path (~lines 2628-2682) repeat the same operation dispatch with byte-identical error strings ('copied capture ...[cpy] requires a copy type; use ...[mov] or ...[cln] instead' at :1206 and :2642; the cln pair at :1237/:2655 and :1248/:2666). The spawn path additionally layers thread-safety checks (type_contains_shared_pointer, capture_spawn_send_error) — intentional today, but the shared per-op core (copy-ability, clone-ability, move marking, error wording) has already required parallel edits and will drift; extract it into one helper parameterized by boundary kind.
- **Editor CLI tool commands panic on JSON-RPC plumbing instead of returning EditorError** — `lang/tooling/fol-editor/src/commands.rs` — The file-driven `fol tool` commands unwrap the internal LSP round-trip with .expect(): semantic tokens (lang/tooling/fol-editor/src/commands.rs:329-338), references (388-394), rename (448-454), completion (516-522). If a handler legitimately returns result: None/null (rename with nothing to rename is null per LSP) or a response shape changes, the CLI panics on a user-supplied file instead of printing an error. These paths already return EditorResult, so mapping to EditorError is mechanical.
- **Dead non-Linux host code paths in a Linux-only-forever project** — `lang/execution/fol-backend/src/emit/build.rs` — Host-conditional compatibility code that can never compile in this greenfield Linux-only project: #[cfg(windows)] pre-delete of the output binary at lang/execution/fol-backend/src/emit/build.rs:794-806, #[cfg(windows)] staged-replacement branch at lang/tooling/fol-editor/src/commands.rs:1265, and #[cfg(not(unix))] exec fallback at lang/tooling/fol-self/src/main.rs:421-427. Per the owner's no-legacy rule these are defects; delete them.
- **38 orphaned package.yaml files in test/ though no code reads package.yaml** — `test/intrinsic_test/package.yaml` — No source in lang/ references package.yaml (metadata comes from build.fol via parse_package_metadata_from_build), yet 38 package.yaml files remain under test/ (e.g. test/intrinsic_test/package.yaml, test/app/build/*/package.yaml, test/apps/fixtures/pkg_basic_import/pkg/math/package.yaml, test/large_examples/*/package.yaml — full list via `find test -name package.yaml`). They are dead fixtures from the replaced metadata format. Also the test name `session_ignores_package_fol_when_package_yaml_is_present` (lang/compiler/fol-resolver/src/session/tests.rs:363) is stale: the test writes package.fol, no yaml is involved.
- **Runtime fault messages inconsistently branded** — `lang/execution/fol-runtime/src/shell.rs` — User-program-reachable runtime faults use the 'fol runtime fault:' prefix in lang/execution/fol-runtime/src/builtins.rs:67-77 (div/mod by zero, overflow), but the same class of fault panics unbranded elsewhere: 'attempted to unwrap nil error shell' at lang/execution/fol-runtime/src/shell.rs:118 and 125, and the eventual double-await/producer-drop expects at lang/execution/fol-runtime/src/std.rs:122-124. Compiled-program crash output therefore looks like an internal Rust panic for some faults and a branded language fault for others.
- **build.fol re-parsed and re-evaluated 3+ times per member per command** — `lang/tooling/fol-frontend/src/compile/mod.rs` — One `check` of a member evaluates the same build.fol through evaluate_package_build at least three times: validate_declared_artifact_roots (lang/tooling/fol-frontend/src/compile/mod.rs:519), package_wide_runtime_contract_for_package -> evaluated_package_capability_contract (line 401 -> 181), and validate_build_dependency_queries (line 1337); build/run add more via build_evaluation_inputs. Each call re-reads and re-executes the build program. Cache the EvaluatedBuildSource per (root, config) within a command.
- **term_cols/term_rows spawn an stty subprocess per call** — `/home/bresilla/data/code/bresilla/fol/lang/execution/fol-runtime/src/std.rs` — term_size (std.rs:479-493) shells out to `stty size` and is called separately by term_cols (496-498) and term_rows (501-503), so a TUI querying both per frame forks two processes per frame. No ioctl(TIOCGWINSZ) and no COLUMNS/LINES fallback. Same pattern in raw_mode (449-462), which additionally uses `stty sane` to leave raw mode — resetting ALL terminal settings rather than undoing 'raw -echo' — and returns the requested state even when stty fails, so callers cannot detect failure.
- **Stale comment: backend role map says hooks are 57..=64 but matches 57..=72** — `/home/bresilla/data/code/bresilla/fol/lang/compiler/fol-intrinsics/src/catalog.rs` — catalog.rs:907 reads '// 8 = echo; 57..=64 = the terminal/OS hooks.' directly above the arm `8 | 57..=72 => Some(IntrinsicBackendRole::RuntimeHook)` (line 908). The comment predates the addition of ids 65-72 and was never updated.
- **IntrinsicCategory::Diagnostic misused for time/env/process/fs hooks** — `/home/bresilla/data/code/bresilla/fol/lang/compiler/fol-intrinsics/src/catalog.rs` — sleep_ms (line 685), now_ms (697), env_var (793), shell (805), dir_list (817), read_file (829) — plus write/read_key/raw_mode/term_cols/term_rows/read_key_ms — are all filed under IntrinsicCategory::Diagnostic, while sibling hooks got real categories (int_to_str/byte_to_str = Conversion, str_sub/str_byte = Query). Sleeping, running a shell command, and reading a file are not diagnostics; the taxonomy was not extended when the hook family grew, so category-based tooling/filtering misreports 12 of the 16 hooks.
- **Unexplained intrinsic id gap 17-20 in the registry** — `/home/bresilla/data/code/bresilla/fol/lang/compiler/fol-intrinsics/src/catalog.rs` — The registry jumps from IntrinsicId::new(16) (catalog.rs:201, de_alloc) to IntrinsicId::new(21) (catalog.rs:212, low) with no comment. Ids 17-20 appear nowhere else in the tree — they are ghost slots from deleted intrinsics. Under the greenfield no-legacy rule the ids should either be renumbered contiguously or the gap documented; today roadmap_for_intrinsic's range arms (e.g. '9 | 10 | 11 | 14 | 15 | 21 | 22') silently encode the hole.
- **Runtime hook functions appended after the #[cfg(test)] module** — `/home/bresilla/data/code/bresilla/fol/lang/execution/fol-runtime/src/std.rs` — env_var, shell, dir_list, read_file (std.rs:790-832) sit AFTER the file's `mod tests` block (lines 530-788), while all the other hooks live before it (lines 340-507). The newest four hooks were bolted onto the end of the file past the test module — and none of the four has a unit test in that module either (str_sub/byte_to_str/read_key are also untested at the Rust level; only echo/render and concurrency primitives are).
- **interop_h7 soft-skips to green when FOL_H7_REQUIRED is unset** — `lang/tooling/fol-frontend/tests/interop_h7.rs` — Lines 42-48 and 234-239: without FOL_H7_REQUIRED the tests eprintln a skip note and return Ok — reported as passed. 'cargo test --workspace' therefore shows H7 green while testing nothing; the real run only happens via 'make test-interop' (Makefile:96-103, sets FOL_H7_REQUIRED=1) which 'make verify' does include. Acceptable today, but anyone gating on cargo test alone gets a false interop signal, and there is no skipped-count surfaced.
- **rg-dependent test has been fixed — stale-needle scan is now in-process (memory note stale)** — `test/integration_tests/integration_editor_and_build.rs` — test_active_repo_surface_keeps_runtime_memo_name_and_no_stale_alloc_refs (line 6408) previously spawned 'rg' and failed in environments without real ripgrep. It now uses the pure-Rust collect_text_matches walker (same file, lines 64-104); repo-wide grep confirms no Command::new("rg")/ripgrep invocation remains anywhere in lang/ or test/. The environment-failure caveat no longer applies; the persisted memory note (rg-test-env-limitation) is outdated.
- **Diagnostic-code parity test never checks exit status of either CLI run** — `test/integration_tests/integration_diagnostics_pipeline.rs` — unified_pipeline_json_and_human_carry_same_diagnostic_code (lines 117-147) runs folc twice on a failing fixture but never asserts a non-zero exit code; the human-mode run is validated solely by stdout containing the code string. If the CLI started exiting 0 on errors (regression) this test would stay green. It is the only run-the-CLI test in test/ that skips status entirely — every other harness (compile_app_expect_success in test/apps/test_apps.rs:112, run_tests.rs helpers, integration_v3_runtime_proofs.rs:164/182/211) checks status.success().
- **Hand-copied std sources inside app fixtures can drift from the shipped std** — `test/apps/fixtures/std_basic_import/std/fmt/lib.fol` — test/apps/fixtures/std_basic_import/std/fmt/lib.fol and test/apps/fixtures/std_namespace_import/std/fmt/math/lib.fol are byte-identical copies of lang/library/std/fmt/math/lib.fol (md5 3e944c71...). If the real std module changes, these fixtures keep passing against a stale copy — nothing asserts equality with the shipped file. Other duplication found (25 duplicate hashes among tracked .fol files, e.g. the same trivial main.fol across 8 examples/build_* dirs, test/parser near-identical pairs like simple_book_default_def.fol vs simple_def_default.fol) is mostly benign boilerplate, but the std copies are the drift risk.
- **fol-editor test suite hard-requires an external tree-sitter CLI with no version guard** — `lang/tooling/fol-editor/src/tree_sitter.rs` — Test helpers at lines 454-505 (run_tree_sitter_query/parse/parse_many/test) spawn Command::new("tree-sitter") with .expect(...), so 'cargo test -p fol-editor' (and 'make test' via --workspace) panics on machines without the CLI. tests.yml installs tree-sitter-cli 0.26.8 pinned (workflow line ~52), but the Makefile has no equivalent check, and 'make verify' does not include the 'tree-test' corpus target (Makefile:114 lists fmt-check lint test interop-check test-interop only) — corpus coverage rides implicitly on these fol-editor unit tests. A locally different CLI version can produce divergent results versus CI. Also note tests.yml pins Rust 1.89.0 while release.yml uses dtolnay/rust-toolchain@stable — released binaries are not built with the toolchain that was tested.
- **make docs TYPE=doxygen is a dead branch: checks for doxygen then does nothing, no Doxyfile exists** — `Makefile` — Makefile:151-152 — the doxygen arm of `docs` only runs `command -v doxygen` and then falls through without invoking doxygen; there is no Doxyfile anywhere in the repo, and a pure-Rust workspace has no C/C++ headers for it anyway. The help text (line 135) still advertises 'TYPE=mdbook|doxygen'. Leftover from the Makefile template — delete the branch and the help mention.
- **flake.nix dev shell cannot run make verify and floats the Rust version** — `flake.nix` — flake.nix:20-29 packages rustc/cargo from nixos-unstable (unpinned — CI tests against exactly 1.89.0 per tests.yml:47-51; no rust-toolchain.toml exists in the repo either) and omits tools the repo gate needs: gcc/binutils (Makefile:98-102 test-interop hard-requires GCC), tree-sitter CLI (tree/tree-test targets, tests.yml installs 0.26.8), and mdbook (docs target, book.yml pins 0.5.3). A nix-shell user cannot run the full `make verify` that CI runs, and may format/lint with a different toolchain than CI.
- **release.yml never checks that the tag matches the workspace version** — `.github/workflows/release.yml` — release.yml:63 derives VERSION purely from the tag name (GITHUB_REF_NAME) and stamps it into asset names, but nothing asserts it equals workspace package.version (Cargo.toml:2, currently 0.2.2). A tag pushed without the version bump ships fol/folc binaries whose --version (CARGO_PKG_VERSION) disagrees with the toolchain directory fol-self installs them into (FOL_HOME/toolchains/v$VERSION), breaking the '//fol X.Y.Z' pin resolution invariant. One guard line (grep the version out of Cargo.toml and compare) closes it. Asset-name parity itself is fine: fol-self main.rs:532-533 builds fol-compiler-and-lib-v{version}-{arch}-{os}.tar.gz which matches release.yml:74 naming and the fol-lang/fol remote.

## REJECTED BY VERIFIERS (3)

- **fol-self (the new toolchain manager) has zero tests** — `lang/tooling/fol-self/src/main.rs` — False as stated: main.rs:721-775 contains a #[cfg(test)] module with 7 unit tests (pin parsing x4, normalize_version_dir, looks_like_version, parse_link_manifest), so 'no unit tests' and 'cargo test --workspace ... runs 0 tests' are both wrong. The true, substantive gap — no tests/ dir, no CARGO_BIN_EXE_fol, no behavioral/integration coverage of install/dispatch/pin resolution — is exactly the other scanner's finding on this file, which is CONFIRMED high; this one is a duplicate with incorrect specifics.
- **Tree-sitter book table claims v3_pointers.txt covers address-of and dereference** — `book/src/050_tooling/400_treesitter.md` — The claim that the corpus and grammar have no such cases is false. lang/tooling/fol-editor/tree-sitter/test/corpus/v3_pointers.txt:144-159 is a case titled "V3 Consuming And Borrowed Pointer Dereference" containing `return [drf]pointer;`, `var inner: ptr[int] = [ref]seed;`, `var outer: ptr[ptr[int]] = [ref]inner;`, `var extracted: ptr[int] = [drf]outer;`, plus a `deref_target` expected node at line 130. grammar.js:252 defines `deref_target: $ => prec.right(4, seq('[', 'drf', ']', ...))` and grammar.js:358-360 lists 'drf' and 'ref' as ownership_option members. The book table is accurate — `[ref]` is the address-of spelling and `[drf]` the dereference; the finding's grep evidently missed them because the ops are spelled as bracket options, not `&`/`*`.
- **make verify never runs the tree-sitter corpus (tree-test) despite CI installing the CLI** — `Makefile` — The premise that the corpus is ungated is false. lang/tooling/fol-editor/src/tree_sitter.rs:1300-1325 has #[test] generated_bundle_executes_real_tree_sitter_corpus_cases, which builds the bundle and shells out to the real CLI via run_tree_sitter_test (:498-504, `Command::new("tree-sitter").arg("test")`), asserting `Total parses: {n}; successful parses: {n}; failed parses: 0;` for every registered case. That is a fol-editor lib test, so `cargo test --workspace` (Makefile:109, inside `test`, inside `verify`) runs it, and tests.yml:54 installs tree-sitter-cli 0.26.8 precisely so it can. I ran it: `cargo test -p fol-editor generated_bundle_executes_real_tree_sitter_corpus_cases` -> "test result: ok. 1 passed; 0 failed". The Makefile tree-test target adds parser regeneration on top, which is a separate concern from corpus coverage.