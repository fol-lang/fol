use std: pkg = {"std"};

fun[] work(fail: bol): int / int = {
    when(fail) {
        case(true) { report 9; }
        * { return 7; }
    }
};

fun[] main(): int = {
    work(true) | async;
    return 0;
};
