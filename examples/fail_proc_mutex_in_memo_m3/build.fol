pro[] build(): non = {
    var build = .build();
    build.meta({ name = "fail_proc_mutex_in_memo_m3", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({ name = "fail-proc-mutex-in-memo-m3", root = "src/main.fol", fol_model = "memo" });
    graph.install(app);
    return;
};
