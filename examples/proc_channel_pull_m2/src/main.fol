use std: pkg = {"std"};

fun[] main(): int = {
    var channel: chn[int];
    var sent: err[int] = 41 | channel[tx];
    var received: opt[int] = channel[rx];
    return std::io::echo_int(received[]);
};
