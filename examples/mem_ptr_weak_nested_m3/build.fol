pro[] build(): non = {
    var build = .build();
    build.meta({ name = "mem_ptr_weak_nested_m3", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "mem-ptr-weak-nested-m3",
        root = "src/main.fol",
        fol_model = "memo",
    });
    graph.install(app);
    return;
};
