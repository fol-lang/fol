pro[] build(): non = {
    var build = .build();
    build.meta({ name = "mem_borrow_param_m2", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "mem-borrow-param-m2",
        root = "src/main.fol",
        fol_model = "core",
    });
    graph.install(app);
    return;
};
