use std: pkg = {"std"};

fun[] main(): int = {
    var channel: chn[int];
    [>]fun()[channel[tx]] = {
        var sent: err[int] = 42 | channel[tx];
    };
    var received: opt[int] = channel[rx];
    return std::io::echo_int(received[]);
};
