use std: pkg = {"std"};

typ Counter: rec = {
    value: int,
};

fun[] inspect(counter: mux[Counter]): int = {
    dfr {
        counter.unlock();
    };
    return 0;
};

fun[] main(): int = {
    var counter: Counter = { value = 1 };
    return inspect(counter);
};
