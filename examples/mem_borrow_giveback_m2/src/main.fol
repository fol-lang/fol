typ Item: rec = {
    value: int,
};

fun[] main(): int = {
    var owner: Item = { value = 7 };
    var[bor] view: Item = #owner;
    var seen: int = view.value;
    !view;
    return owner.value + seen;
};
