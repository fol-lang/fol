pro[] build(): non = {
    var build = .build();
    build.meta({ name = "mem_shell_access_m3", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "mem-shell-access-m3",
        root = "src/main.fol",
        fol_model = "memo",
    });
    graph.install(app);
    return;
};
