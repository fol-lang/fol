pro[] build(): non = {
    var build = .build();
    build.meta({ name = "fail_mem_fin_partial_move_m1", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "fail-mem-fin-partial-move-m1",
        root = "src/main.fol",
        fol_model = "memo",
    });
    graph.install(app);
    return;
};
