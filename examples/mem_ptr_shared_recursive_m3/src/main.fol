typ Node: rec = {
    value: int,
    next: opt ptr[shared, Node],
};

fun[] main(): int = {
    var tail: Node = { value = 2, next = nil };
    var tail_ptr: ptr[shared, Node] = [ref]tail;
    var head: Node = { value = 1, next = tail_ptr };
    return ([drf](head.next[])).value;
};
