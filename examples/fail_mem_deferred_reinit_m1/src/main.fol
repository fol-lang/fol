fun[] consume(pointer: ptr[int]): int = {
    return [drf]pointer;
};

fun[] main(): int = {
    var first: int = 1;
    var second: int = 2;
    var[mut] pointer: ptr[int] = [ref]first;
    consume([mov]pointer);
    dfr {
        pointer = [ref]second;
    };
    return 0;
};
