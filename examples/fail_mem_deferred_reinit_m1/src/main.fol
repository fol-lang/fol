fun[] consume(pointer: ptr[int]): int = {
    return *pointer;
};

fun[] main(): int = {
    var first: int = 1;
    var second: int = 2;
    var[mut] pointer: ptr[int] = &first;
    consume(pointer);
    dfr {
        pointer = &second;
    };
    return 0;
};
