fun[] consume(value: ptr[shared, int]): int = {
    return [drf]value;
};

fun[] main(): int = {
    var value: int = 7;
    var shared: ptr[shared, int] = [ref]value;
    [>]consume(shared);
    return 0;
};
