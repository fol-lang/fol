pro[] build(): non = {
    var build = .build();
    build.meta({ name = "core_dfr", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "core-dfr",
        root = "src/main.fol",
        fol_model = "core",
    });
    graph.install(app);
    return;
};
