pro[] build(): non = {
    var build = .build();
    build.meta({ name = "fail_mem_share_field_m2", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "fail-mem-share-field-m2",
        root = "src/main.fol",
        fol_model = "memo",
    });
    graph.install(app);
    return;
};
