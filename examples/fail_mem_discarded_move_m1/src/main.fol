fun[] consume(pointer: ptr[int]): int = {
    return [drf]pointer;
};

fun[] main(): int = {
    var value: int = 7;
    var pointer: ptr[int] = [ref]value;
    pointer;
    return consume(pointer);
};
