pro[] build(): non = {
    var build = .build();
    build.meta({ name = "fail_generic_receiver_m1", version = "0.1.0" });

    var graph = build.graph();
    graph.add_exe({
        name = "fail_generic_receiver_m1",
        root = "src/main.fol",
        fol_model = "memo",
    });
    return;
};
