typ Holder: rec = {
    link: ptr[int],
};

fun[] inspect(holder: Holder): int = {
    return [drf]holder.link;
};

fun[] main(): int = {
    var value: int = 7;
    var holder: Holder = { link = [ref]value };
    return inspect([mov]holder);
};
