pro[] build(): non = {
    var build = .build();
    build.meta({
        name = "fail_standard_import_ambiguity_m2",
        version = "0.1.0",
    });

    var graph = build.graph();
    graph.add_exe({
        name = "fail_standard_import_ambiguity_m2",
        root = "src/main.fol",
        fol_model = "memo",
    });
    return;
};
