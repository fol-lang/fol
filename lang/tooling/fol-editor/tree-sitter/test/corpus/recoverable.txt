==================
Recoverable
==================

use shared: loc = {"../shared"};

fun load(flag: bol): int / str = {
    when(flag) {
        case(true) { report "bad-input"; }
        * { return 7; }
    }
};

fun demo(flag: bol): int = {
    return load(flag) || 0;
};
