pro[] build(): non = {
    var build = .build();
    build.meta({
        name = "redis_store", version = "0.1.0", kind = "exe",
        description = "mini-redis: RESP codec, expiring key-value store, command dispatch",
        license = "MIT",
    });
    build.add_dep({ alias = "std", source = "internal", target = "standard" });
    var graph = build.graph();
    var target = graph.standard_target();
    var optimize = graph.standard_optimize();
    var app = graph.add_exe({
        name = "redis_store", root = "src/main.fol", fol_model = "memo",
        target = target, optimize = optimize,
    });
    graph.install(app);
    graph.add_run(app);
    return;
};
