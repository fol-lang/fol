pro[] build(): non = {
    var build = .build();
    build.meta({
        name = "json_tokens", version = "0.1.0", kind = "exe",
        description = "jsmn, the zero-allocation JSON tokenizer, ported to FOL",
        license = "MIT",
    });
    build.add_dep({ alias = "std", source = "internal", target = "standard" });
    var graph = build.graph();
    var target = graph.standard_target();
    var optimize = graph.standard_optimize();
    var app = graph.add_exe({
        name = "json_tokens", root = "src/main.fol", fol_model = "memo",
        target = target, optimize = optimize,
    });
    graph.install(app);
    graph.add_run(app);
    return;
};
