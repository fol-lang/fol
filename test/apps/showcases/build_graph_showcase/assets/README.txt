manifest-audit
==============

Audits a release manifest against the policy generated by the build graph.
Each entry is `name:bytes:channel`; violations are reported per entry and the
process exits non-zero when anything may not ship.
