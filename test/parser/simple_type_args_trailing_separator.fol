typ Numbers: seq[int,];
typ Lookup: map[str, vec[int,],];

fun build(items: seq[pkg::Input,]): map[str, vec[pkg::Output,],] = {
    var[imu] cache: map[str, vec[int,],] = source;
    return cache;
};
