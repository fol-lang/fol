pro main(): int = {
    var channel: chn[str];
    var launched = [>]doItFast() | channel[tx];
    select {
        when channel as value { .echo(value); }
    };
};

fun loadMesh(path: str, meshes[mux]: vec[mesh]): int = {
    return 1;
};
